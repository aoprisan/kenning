/** [moduleId, text, options, correctIndex, explanation] */
export const questions = [
    // k1
    ["k1", "What does Kerckhoffs's principle actually require?", ["That the algorithm be published", "That the system stay secure when everything except the key is public", "That the key be at least 128 bits", "That the source code be audited"], 1, "It is a design requirement about where the secret lives, not a licensing position: the key is the only part you can rotate after a disclosure, so it must be the only part you rely on. Publishing the algorithm is a common consequence, not the requirement; key length and audits are separate concerns entirely."],
    ["k1", "Why can no deterministic encryption scheme be IND-CPA secure?", ["Because determinism makes it slower to compute", "Because the adversary can encrypt a candidate plaintext and compare the ciphertexts", "Because the key must then be reused", "Because it cannot handle messages longer than one block"], 1, "In the indistinguishability game the adversary picks the two candidates, so it can encrypt one itself under the public rules and check for a match, winning with certainty. Speed is irrelevant to the definition, key reuse is a separate issue, and message length is handled by the mode."],
    ["k1", "A system encrypts a database column so it can still be indexed, using the same key and no randomisation. What leaks?", ["Nothing, because AES is secure", "The complete equality structure of the column", "Only the number of rows", "The key, after enough rows"], 1, "Equal plaintexts give equal ciphertexts, so every repeat is visible; combined with a public frequency distribution over diagnoses or salaries that is usually the whole secret. AES itself is untouched, which is exactly the point — the mode and the determinism leak, not the cipher. Row count is visible anyway, and the key does not leak."],
    ["k1", "RSA-2048 has a 2048-bit key. What is its security level?", ["2048 bits", "1024 bits", "About 112 bits", "256 bits"], 2, "Security level is the cost of the best known attack, and factoring a 2048-bit modulus is far cheaper than searching a 2048-bit space — it comes to roughly 112 bits, with RSA-3072 needed for 128. Halving the key length is not the rule either; the relationship is not linear."],
    ["k1", "Which capability does a chosen-ciphertext adversary actually need?", ["Access to the private key", "A decryption service that returns plaintext", "Any observable difference in behaviour depending on what a decryption produced", "The ability to choose the encryption key"], 2, "One bit of feedback is enough — an error string, a status code, a response length, a timing gap. Access to the key or to full plaintexts would be a much stronger and much rarer position, and choosing the key is not part of the model at all."],
    ["k1", "A design uses AES-256 with a key derived from a six-digit PIN. What is its real strength?", ["256 bits, because AES-256 is used", "About 20 bits, the entropy of the PIN", "128 bits, halved by the derivation", "Depends on the KDF's iteration count alone"], 1, "Security is the weakest link: a million possible PINs is about twenty bits, and no cipher can create entropy that the key never had. The cipher's own strength is irrelevant here, nothing halves it, and a KDF raises the cost per guess but does not enlarge the search space."],
    ["k1", "Which of these is the most common cause of a real cryptographic failure?", ["A cryptanalytic break of the primitive", "Misuse: bad randomness, reuse, missing authentication or a leaking error path", "Insufficient key length", "A flaw in the standards process"], 1, "AES and SHA-256 have stood for two decades with no practical break, while deployed systems fail constantly through the surrounding decisions. Key length is almost never the binding constraint, and the open standards processes have a much better record than the systems built on top of them."],
    // k2
    ["k2", "How much entropy does a 256-bit key have if it was generated by seeding a PRNG with the current second?", ["256 bits", "128 bits", "About as many bits as there are plausible seconds — roughly 25 for a year's range", "Zero, because the key is deterministic"], 2, "Entropy belongs to the generating distribution, not to the length of the output: an attacker only has to try the plausible seeds. Length says nothing here, and it is not zero — the attacker still has to search the seed range, which is small but not empty."],
    ["k2", "Why is the Mersenne Twister unsuitable for generating keys?", ["Its output is statistically biased", "Its internal state, and so all future output, is recoverable from 624 outputs", "It is too slow for bulk generation", "It has a short period"], 1, "It passes statistical tests and has an enormous period; the disqualifying property is predictability, which is the only one that matters for a key. Bias and speed are not the problem, and neither is period length."],
    ["k2", "On a modern Linux system, when should you prefer /dev/random over /dev/urandom?", ["Always, because it blocks until entropy is available", "For long-term keys only", "Never — once the pool is initialised they are cryptographically identical", "When generating more than 256 bits at a time"], 2, "After initialisation the two are the same generator, and blocking buys nothing but stalls. The genuine hazard is different: generating keys before the pool is initialised, which is what broke a generation of headless embedded devices."],
    ["k2", "What was the actual cause of the 2008 Debian OpenSSL key disaster?", ["A weakness found in RSA", "A line feeding the entropy pool was removed to silence a memory-checking tool", "The key length was too short", "A backdoored prime was distributed"], 1, "With that input gone, only the process id varied, leaving 32,768 possible keys — and every test still passed, because the output looked random. RSA was not weakened, the key sizes were normal, and no parameters were tampered with."],
    ["k2", "Which requirement does a CBC initialisation vector have that a CTR nonce does not?", ["It must be secret", "It must be unpredictable to the attacker before encryption", "It must be the same length as the key", "It must never be transmitted"], 1, "Uniqueness is common to both; CBC additionally needs unpredictability, and a predictable IV is the BEAST attack against TLS 1.0. Neither is secret, neither is tied to key length, and both are sent in the clear."],
    ["k2", "Why is a counter usually a safer nonce source than a random value?", ["Counters are harder for an attacker to guess", "A counter cannot collide by accident, while random values collide at the birthday bound", "Counters are faster to generate", "Random values are biased in practice"], 1, "Nonces need uniqueness, not unpredictability, and a counter guarantees it outright; random 96-bit nonces start colliding around 2³² messages. Predictability is not a problem for a nonce, and neither speed nor bias is the reason."],
    ["k2", "With random 64-bit IVs, roughly how many messages under one key before a collision becomes likely?", ["2⁶⁴", "2³²", "2¹⁶", "2⁴⁸"], 1, "The birthday bound puts collisions near the square root of the space, so about four billion messages — reachable by a busy server. 2⁶⁴ is the size of the space, not the collision point, and the other two are neither."],
    ["k2", "What is a salt required to be?", ["Secret and unpredictable", "Unique per record, and it may be stored in the clear", "The same for all records, so hashes can be compared", "At least as long as the password"], 1, "A salt defeats precomputation and hides equal passwords; it does neither of those things if it is shared, and it needs no secrecy at all, which is why it is stored next to the hash. Its length is unrelated to the password's."],
    // k3
    ["k3", "What does Shannon's proof of the one-time pad's perfect secrecy also establish?", ["That XOR is the strongest operation available", "That the key must be at least as long as the message", "That the pad resists quantum attack", "That the key must be changed daily"], 1, "Perfect secrecy requires the key space to be at least the size of the message space, which is exactly what makes the pad impractical. XOR is not special in itself, quantum resistance is a consequence rather than the theorem, and there is no rule about daily rotation — the rule is one use."],
    ["k3", "Two messages are encrypted with the same keystream. What can an eavesdropper compute with no key?", ["Nothing useful", "m₁ ⊕ m₂", "The keystream itself", "One of the plaintexts, but not the other"], 1, "The keystream cancels: c₁ ⊕ c₂ = m₁ ⊕ m₂. Plaintext redundancy then recovers both by crib dragging, so \"nothing useful\" and \"only one\" are both wrong; the keystream itself is not directly recoverable without knowing a plaintext."],
    ["k3", "Why does the XOR of two English plaintexts leak so much structure?", ["Because English has more vowels than consonants", "Because ASCII letters share their top bits, so those positions are always zero", "Because XOR is not a random operation", "Because message lengths differ"], 1, "Every ASCII letter starts 010, so the XOR of two letters has three guaranteed zero bits per byte — a regularity that immediately separates letters from spaces and gives crib dragging its foothold. Vowel counts are a much weaker signal, XOR is perfectly uniform on uniform inputs, and length is not the mechanism."],
    ["k3", "WEP used a 24-bit IV with a fixed key. Why did that fail?", ["24 bits is too short to encrypt", "The birthday bound makes IVs repeat after a few thousand frames", "The IV was secret and leaked", "RC4 rejects short IVs"], 1, "Repeat the IV and you repeat the keystream, which is the two-time pad — and 2¹² is a few thousand frames, minutes of traffic. The IV is not the encryption and is not secret, and RC4 accepted it fine; the size was the problem."],
    ["k3", "An attacker knows a stream-cipher ciphertext encrypts \"role=user\" at a known offset. What can they do without the key?", ["Nothing, since they cannot decrypt", "Change it to \"role=root\" undetectably", "Recover the key from the known plaintext", "Only corrupt the message at random"], 1, "XOR in (known ⊕ desired) at that offset and the plaintext becomes the desired value; there is nothing in the scheme to detect the change. The key is not recoverable from one known plaintext block, and the modification is precise, not random."],
    ["k3", "What does a stream cipher provide?", ["Confidentiality and integrity", "Confidentiality only", "Integrity only", "Confidentiality, integrity and authentication"], 1, "It hides content and detects nothing — every ciphertext bit flip is an undetected plaintext bit flip. That is why the primitive to actually deploy is ChaCha20-Poly1305 rather than bare ChaCha20."],
    ["k3", "What did VENONA exploit?", ["A weakness in the one-time pad's mathematics", "Duplicated pad pages, so the same key material encrypted two messages", "Poor Soviet key length choices", "An intercepted key distribution channel"], 1, "Each page was individually unbreakable; wartime production pressure duplicated pages, and the reuse is what US cryptanalysts read for decades. The mathematics is provably sound, the key was full-length, and the distribution channel was not compromised."],
    // k4
    ["k4", "What is a block cipher, precisely?", ["An encryption scheme for messages of any length", "A keyed permutation on a fixed-width block", "A one-way function on a fixed-width block", "A keystream generator"], 1, "It is invertible — the key holder must be able to get back — and it handles exactly one block, which is why it needs a mode to become a scheme. A one-way function is a hash, and a keystream generator is what CTR mode turns it into."],
    ["k4", "Why does the outline of an image survive ECB encryption?", ["AES is weak on image data", "Equal plaintext blocks map to equal ciphertext blocks, so the pattern is never encrypted", "The image header is left in the clear", "The block size is smaller than a pixel"], 1, "ECB encrypts each block independently and deterministically, preserving the plaintext's whole equality structure — the cipher is not weakened at all. Header handling is a separate detail, and block size relative to a pixel is not what produces the effect."],
    ["k4", "TLS 1.0 chained the CBC IV from the previous record's last ciphertext block. What did that break?", ["Uniqueness of the IV", "Unpredictability of the IV, which is the BEAST attack", "The MAC over the record", "The padding check"], 1, "The IVs were unique but the attacker could see the previous block and therefore predict the next IV, letting them confirm guesses about adjacent secret bytes. The MAC and the padding check are separate mechanisms with their own attacks."],
    ["k4", "Which property does CTR mode have that CBC does not?", ["It authenticates the ciphertext", "Encryption and decryption are both fully parallel, with no padding", "It resists nonce reuse", "It uses both directions of the block cipher"], 1, "CTR produces a keystream, so any block can be computed independently and the ciphertext is exactly the plaintext's length. It authenticates nothing, is catastrophically broken by nonce reuse, and uses only the forward direction of the cipher."],
    ["k4", "What do ECB, CBC and CTR have in common?", ["All are deterministic", "All require padding", "None provides any integrity, and all are malleable", "All are parallelisable"], 2, "Confidentiality without authentication is what unites them, and it is the most common design error in applied cryptography. Only ECB is deterministic, only ECB and CBC need padding, and CBC encryption is sequential."],
    ["k4", "Why were 64-bit block ciphers such as 3DES retired for bulk traffic?", ["A cryptanalytic break of the cipher", "Ciphertext blocks collide after about 2³² blocks, which is around 32 GB under one key", "Their keys were too short for modern hardware", "They could not be implemented in constant time"], 1, "That is Sweet32, and it follows from the birthday bound on the block width rather than from any weakness in 3DES itself. Key length is a separate criticism of single DES, and constant-time implementation was not the reason."],
    ["k4", "In the animation, changing the IV under ECB changes nothing. Why?", ["The animation ignores it", "ECB has no chaining or keystream, so there is nowhere for an IV to enter", "The IV is applied only to the first block", "ECB encrypts the IV separately"], 1, "Each block is encrypted independently under the key alone, so the mode simply has no input for an IV — which is the same fact that makes it deterministic and structure-preserving."],
    // k5
    ["k5", "Why does PKCS#7 add a whole extra block when the message is already block-aligned?", ["To increase the security margin", "So that unpadding is unambiguous", "To carry the IV", "To align with the MAC's block size"], 1, "Without it, a message legitimately ending in 0x01 could not be distinguished from one byte of padding. It adds no security, carries nothing, and has nothing to do with the MAC."],
    ["k5", "What exactly is a padding oracle?", ["A service that decrypts arbitrary ciphertexts", "Any observable difference between a padding failure and a later failure", "A weakness in PKCS#7 itself", "A timing attack against the block cipher"], 1, "An error string, a status code, a response length or a few microseconds all serve. No decryption service is needed, PKCS#7 itself is a correct encoding, and the cipher's own timing is not what leaks."],
    ["k5", "After the oracle accepts a forged block r with last byte producing valid single-byte padding, what does the attacker know?", ["The key's last byte", "D(c)[15] = r[15] ⊕ 0x01", "The plaintext's last byte directly", "The IV"], 1, "They learn the raw block-cipher decryption of that byte; the plaintext byte then follows by XORing with the real preceding ciphertext block. No key material is recovered at any point, and the IV is already public."],
    ["k5", "Roughly what does a padding oracle attack cost per plaintext byte?", ["2⁶⁴ operations", "At most 256 queries", "One query", "2¹²⁸ operations"], 1, "One byte value out of 256 produces valid padding, so the attacker walks the range — about 128 queries on average, 256 at worst, and a few thousand per block. The exponential figures describe brute force, which is not what is happening here."],
    ["k5", "Why did Lucky Thirteen still work against implementations that had equalised their error messages?", ["The error codes still differed", "The MAC ran over a different number of bytes depending on the padding stripped, leaving a timing gap", "The padding check was skipped entirely", "The attacker had the key"], 1, "A few microseconds of difference was enough over a network, which is precisely why cosmetic equalisation is not the fix. The codes were equal, the check ran, and no key was involved."],
    ["k5", "What is the structural fix for padding oracles?", ["Uniform error messages", "Encrypt-then-MAC, verifying the tag before any decryption — or an AEAD", "Longer padding", "Randomising the padding bytes"], 1, "If a forged ciphertext is rejected before the cipher touches it, the padding check can never run on attacker-controlled data and so can never leak. Uniform messages were tried and defeated by timing; padding length and content do not address the oracle at all."],
    ["k5", "What is the general lesson beyond padding?", ["PKCS#7 should not be used", "Any distinguishable decryption failure is a decryption oracle", "CBC should never be used", "Errors should never be returned to clients"], 1, "The channel happened to be padding validity; it could equally be a signature check, a parse failure, or a downstream behaviour. PKCS#7 and CBC are usable when authentication comes first, and clients do need errors — they need ones that do not depend on decrypted content."],
    // k6
    ["k6", "SHA-256 produces a 256-bit digest. What is its collision resistance?", ["256 bits", "128 bits", "192 bits", "It depends on the input length"], 1, "Collisions cost about 2^(n/2) by the birthday bound, because the attacker picks both messages and needs only some pair to match. Preimage resistance is the full 256 bits, and input length does not enter into it."],
    ["k6", "Verifying a downloaded file against a digest published on a trusted site relies on which property?", ["Collision resistance", "Second-preimage resistance", "Preimage resistance", "Length-extension resistance"], 1, "The file existed before the attacker chose anything, so they must find a second input matching a fixed digest. Collision resistance matters when the attacker chose both messages, as with a submitted certificate request; preimage resistance is about inverting a digest, which is not the task here."],
    ["k6", "Why is H(secret ‖ message) a broken MAC?", ["Because SHA-256 has known collisions", "Because the digest is the whole internal state, so anyone can extend the message", "Because the secret must come last", "Because hashes are not keyed"], 1, "Length extension lets an attacker holding the tag and the length append data and produce a valid tag for the longer message, with no knowledge of the secret. SHA-256 has no practical collisions; putting the secret last avoids extension but makes a collision into a forgery; and the construction is keyed, just badly."],
    ["k6", "Which of these is immune to length extension?", ["SHA-1", "SHA-256", "SHA-512", "HMAC-SHA256"], 3, "HMAC's nested structure means the tag is an output of the outer hash, with nothing to extend. All three bare Merkle–Damgård hashes listed are extendable — SHA-3, BLAKE2 and SHA-512/256 would also be immune, by other means."],
    ["k6", "MD5 collisions are trivial. Why are leaked MD5 password hashes still attacked by guessing rather than by inversion?", ["Because the passwords are salted", "Because MD5's preimage resistance is not broken — only collision resistance is", "Because guessing is faster than a collision search", "Because MD5 output is too short to invert"], 1, "Collisions and preimages are different properties, and only the former has fallen. Salting is often absent in exactly these breaches, guessing is not inherently faster, and output length is not what prevents inversion."],
    ["k6", "What did the Flame malware use a chosen-prefix MD5 collision for?", ["To decrypt Windows Update traffic", "To forge a Microsoft code-signing certificate", "To recover a signing key", "To bypass a password check"], 1, "Two meaningful prefixes with matching digests let a signature over one document be presented as a signature over the other, producing a certificate that Windows trusted. No key was recovered and nothing was decrypted."],
    ["k6", "A service publishes SHA-256 hashes of user phone numbers as \"anonymised\" identifiers. What is wrong?", ["SHA-256 is too fast for this use", "The input space is small enough to enumerate and match exhaustively", "The hashes will collide", "Phone numbers need a longer digest"], 1, "There are only so many phone numbers; hashing all of them is cheap, so the mapping is fully reversible in bulk. A hash hides only high-entropy inputs. Speed is a symptom rather than the cause, collisions are irrelevant here, and digest length changes nothing."],
    // k7
    ["k7", "What does a MAC give that a signature does not?", ["Public verifiability", "Nothing — a signature is strictly stronger in what it provides", "Non-repudiation", "Protection against replay"], 1, "A MAC provides integrity and authenticity between parties sharing a key; a signature provides that plus public verifiability and non-repudiation. Neither provides replay protection on its own — that needs freshness."],
    ["k7", "Why is HMAC-SHA1 still considered unbroken despite practical SHA-1 collisions?", ["HMAC truncates the output", "HMAC's security proof rests on assumptions weaker than collision resistance", "SHA-1 collisions require chosen prefixes", "The key makes collisions impossible"], 1, "The nested construction lets HMAC rely on weaker properties of the compression function, so a collision attack does not translate into a forgery. It does not truncate, chosen-prefix collisions are also practical now, and a key does not make collisions impossible."],
    ["k7", "Which composition order forces the receiver to decrypt attacker-supplied data before it can authenticate it?", ["Encrypt-then-MAC", "MAC-then-Encrypt", "Encrypt-and-MAC", "All three"], 1, "The tag is inside the ciphertext, so decryption must happen first — which is the precondition for Lucky Thirteen and POODLE. Encrypt-then-MAC verifies first, and encrypt-and-MAC has a separate problem: it leaks plaintext equality through the tag."],
    ["k7", "In encrypt-then-MAC, what must the tag cover?", ["The ciphertext only", "The ciphertext and the IV", "The plaintext and the ciphertext", "The key and the ciphertext"], 1, "An unauthenticated IV lets an attacker rewrite the first plaintext block of a CBC message undetected. Covering the plaintext would be a different construction, and a MAC never covers its own key."],
    ["k7", "What happens when an AES-GCM (key, nonce) pair is reused for two messages?", ["The two plaintexts leak, and nothing else", "The authentication subkey can be recovered, so the attacker can forge arbitrary messages under that key", "Decryption fails on both", "Only the shorter message leaks"], 1, "Beyond the two-time pad on the plaintexts, two polynomial evaluations under one nonce solve for the secret authentication key, giving forgery for every future message. That is why it is called the forbidden attack rather than a degradation."],
    ["k7", "What is associated data for?", ["Padding the plaintext to a block boundary", "Authenticating context that is not encrypted, such as headers and sequence numbers", "Storing the nonce", "Providing forward secrecy"], 1, "It binds the ciphertext to where it belongs, so a valid ciphertext replayed into another row, session or version is rejected. The nonce is a separate parameter, and AD has nothing to do with padding or forward secrecy."],
    ["k7", "Why must tag comparison be constant-time?", ["To keep verification fast", "Because an early return leaks the matching prefix, reducing forgery from 2¹²⁸ to about 4,096 attempts", "Because the tag may contain the key", "To avoid cache misses"], 1, "The timing tells the attacker how many leading bytes were right, so they can find the tag one byte at a time. Constant-time comparison is not faster, the tag contains no key material, and cache behaviour is a separate channel."],
    ["k7", "Which AEAD is designed so that random nonces are safe without a message counter?", ["AES-GCM", "ChaCha20-Poly1305", "XChaCha20-Poly1305", "AES-CBC-HMAC"], 2, "Its 192-bit nonce puts the birthday collision point far out of reach, which is the point of the extended construction. AES-GCM and ChaCha20-Poly1305 use 96-bit nonces with a practical message limit, and AES-CBC-HMAC is not an AEAD primitive at all."],
    // k8
    ["k8", "What does an eavesdropper on a Diffie–Hellman exchange see?", ["g, g^a and g^b", "g^(ab)", "a and b", "Nothing, the exchange is encrypted"], 0, "The public values cross the wire in the clear; combining them into g^(ab) is the computational Diffie–Hellman problem. The secret exponents never leave their machines, and the exchange itself is not encrypted — that is what it establishes."],
    ["k8", "Why is the raw Diffie–Hellman shared secret never used directly as a key?", ["It is too short", "It is not uniformly distributed, and DDH may not hold in the group", "It is known to the eavesdropper", "It changes on each use"], 1, "A group element is not uniform bytes, and indistinguishability from random is a stronger assumption than the exchange needs, so the value goes through a KDF with a context string. It is neither short nor known to the eavesdropper, and it is stable for a given pair of exponents."],
    ["k8", "Unauthenticated Diffie–Hellman is secure against which adversary?", ["An active attacker who can modify traffic", "A passive eavesdropper only", "Both", "Neither"], 1, "Against a passive attacker it is excellent; an active one runs two exchanges and relays, and both parties complete the handshake with no anomaly. That is why every deployed protocol binds the exchange to an identity."],
    ["k8", "How does TLS 1.3 stop a man-in-the-middle on its key exchange?", ["By encrypting the key shares", "By signing the whole handshake transcript with the certified long-term key", "By using larger groups", "By comparing fingerprints out of band"], 1, "A substituted key share changes the transcript, and the attacker cannot produce a signature over the changed version. Encrypting the shares would need a key that does not yet exist, group size does not address an active attacker, and out-of-band comparison is a different mechanism used by messengers."],
    ["k8", "What does using ephemeral exponents buy?", ["Faster handshakes", "Forward secrecy: recorded traffic stays safe if the long-term key is later compromised", "Protection against a man in the middle", "Smaller key shares"], 1, "The exponents are deleted with the session, so there is nothing left to decrypt the recording with. It costs rather than saves time, does nothing about an active attacker, and does not change the sizes."],
    ["k8", "Why does finite-field Diffie–Hellman need about 3072 bits where an elliptic curve needs 256?", ["Curves use a stronger cipher", "Index calculus applies to finite fields but not to elliptic curves", "Curve arithmetic is faster", "Finite fields have smaller groups"], 1, "With only generic square-root attacks available on curves, the group order can be roughly twice the security level; index calculus makes finite fields far weaker than their size suggests. Speed is a consequence, not the cause, and the field's group is the larger one."],
    ["k8", "What was the deeper lesson of Logjam, beyond the export-grade groups?", ["Downgrade attacks are unavoidable", "Precomputation depends only on the prime, so one effort breaks every session using a shared group", "512-bit primes are too small", "DH should be replaced by RSA"], 1, "Because almost everyone used a handful of standard primes, a single precomputation against a common 1024-bit group would compromise everything using it. Downgrades are preventable by authenticating the negotiation, the small-group point is the surface finding, and RSA key transport is strictly worse — it has no forward secrecy."],
    ["k8", "Why should a received Diffie–Hellman public value be validated?", ["To confirm the peer's identity", "Because a small-order or off-curve point can force a shared secret with very few possible values and leak the private key", "To prevent replay", "To check the protocol version"], 1, "Small-subgroup and invalid-curve attacks recover the private exponent over a few handshakes, especially when an \"ephemeral\" exponent is reused. Validation says nothing about identity, replay or version — those need other mechanisms."],
    // k9
    ["k9", "In RSA, what is the private exponent d?", ["A large random prime", "The modular inverse of e modulo φ(n)", "The product of p and q", "The discrete logarithm of e"], 1, "Because ed ≡ 1 (mod φ(n)), the two exponentiations undo each other. The product of the primes is n, d is not itself prime, and no discrete logarithm appears in RSA."],
    ["k9", "Textbook RSA encrypts a session identifier drawn from a set of a thousand possibilities. What can an attacker do?", ["Nothing without the private key", "Encrypt all thousand candidates under the public key and compare", "Factor n from the ciphertext", "Recover d by repeated encryption"], 1, "Encryption is deterministic and the public key is public, so a small message space is enumerated directly — no cryptanalysis at all. Nothing about the ciphertext helps factor n, and repeated encryption does not reveal d."],
    ["k9", "What does RSA's malleability mean concretely?", ["Ciphertexts can be reordered", "Multiplying a ciphertext by r^e multiplies the plaintext by r", "Ciphertexts decrypt to multiple plaintexts", "The modulus can be changed"], 1, "The homomorphism (m·r)^e = m^e·r^e lets an attacker transform ciphertexts blindly, and underpins blinding attacks on signing. Decryption is a bijection so there is one plaintext, and neither ordering nor the modulus is involved."],
    ["k9", "With e = 3 and no padding, when is a ciphertext trivially decryptable?", ["When n is small", "When m³ < n, so no modular reduction happens and the integer cube root recovers m", "When m is even", "Never — e = 3 is safe"], 1, "Without reduction the operation is an ordinary integer cube, invertible by anyone. The size of n is not itself the issue, parity is irrelevant, and small exponents with proper padding are fine — it is the missing padding that breaks it."],
    ["k9", "What does Bleichenbacher's attack need from the server?", ["The private key", "One bit indicating whether PKCS#1 v1.5 padding was valid", "A signature over a chosen message", "The ability to choose the modulus"], 1, "That single bit, combined with RSA's malleability, narrows the interval containing the plaintext until it closes. Nothing about the key or the modulus is needed, and it targets encryption rather than a signing oracle."],
    ["k9", "Why is \"a signature is encryption with the private key\" a harmful description?", ["It is too informal", "It describes only raw RSA, and encourages reusing one key pair for both jobs", "Signatures are actually slower", "It confuses hashing with padding"], 1, "It does not describe PSS, ECDSA or Ed25519 at all, and it leads to designs where a decryption oracle becomes a signing oracle. Relative speed is not the objection, and the hashing step is a separate point that the gloss also omits."],
    ["k9", "How were hundreds of thousands of RSA keys broken in 2012 without any cryptanalysis?", ["A flaw in the primality test", "Pairwise GCDs over collected moduli found shared primes from starved entropy pools", "A backdoor in the standard", "Brute-force factoring of 1024-bit keys"], 1, "Two devices generating keys with no entropy sometimes picked the same prime, and a GCD over public moduli factored both instantly. No standard was compromised, primality testing was fine, and 1024-bit factoring by brute force was not involved."],
    ["k9", "Which pair of padding schemes should modern RSA use?", ["PKCS#1 v1.5 for both", "OAEP for encryption, PSS for signatures", "OAEP for both", "No padding, with a hash instead"], 1, "They are the schemes with security proofs, and PKCS#1 v1.5 encryption remains Bleichenbacher-vulnerable — ROBOT found it live nineteen years after publication. OAEP is not a signature scheme, and hashing without padding is not a substitute."],
    // k10
    ["k10", "Why does a 256-bit elliptic curve give roughly the same security as a 3072-bit RSA modulus?", ["Curve arithmetic is harder to implement", "No index calculus attack is known for curves, so only generic square-root attacks apply", "Curves use larger internal state", "RSA wastes bits on the padding"], 1, "With Pollard rho at √n as the best method, the group order need only be about twice the security level. Implementation difficulty is not a security parameter, internal state is not the measure, and padding overhead is negligible."],
    ["k10", "Two ECDSA signatures use the same nonce k. What follows?", ["The signatures are identical", "The private key can be computed by arithmetic from the two signatures", "The messages must have been identical", "Nothing, provided the messages differ"], 1, "k = (H(m₁) − H(m₂))/(s₁ − s₂), then d = (s₁k − H(m₁))/r — microseconds of modular arithmetic. The signatures share r but differ in s, and different messages make the attack possible rather than preventing it."],
    ["k10", "How does an attacker spot nonce reuse in published signatures?", ["By timing the signer", "By looking for a repeated r value", "By comparing message hashes", "They cannot; it must be inferred from the key"], 1, "r is the x-coordinate of kG, so the same k always yields the same r — which is how blockchains were scanned for vulnerable wallets in 2013. Timing is a different channel, and message hashes differ by construction."],
    ["k10", "Is a merely biased nonce safe?", ["Yes, as long as it never repeats", "No — a few leaked bits over a few hundred signatures let a lattice attack recover the key", "Yes, if the bias is under one percent", "Only for Ed25519"], 1, "Each signature becomes a noisy linear equation in the private key, and lattice reduction solves the resulting hidden number problem; Minerva and TPM-FAIL did exactly this from timing leaks revealing only the nonce's bit length. Ed25519 avoids the issue by being deterministic, not by tolerating bias."],
    ["k10", "What does RFC 6979 change?", ["The curve used", "The nonce is derived deterministically as an HMAC of the private key and message hash", "The signature encoding", "The hash function"], 1, "It removes the random number generator from the failure path entirely while keeping the nonce unpredictable to anyone without the key. It changes neither the curve, the encoding, nor the hash."],
    ["k10", "What is the one cost of Ed25519's deterministic nonce?", ["Signatures become larger", "It is vulnerable to fault injection, since two signatures over one message should be identical", "Verification becomes slower", "It requires a random seed at each signature"], 1, "A glitched signature over a message whose correct signature is known reveals information, which is why hedged variants mix in randomness. Sizes and verification cost are unchanged, and the whole point is that no per-signature randomness is needed."],
    ["k10", "Why should a signature include a domain separator?", ["To reduce the signature size", "So the same signed bytes cannot be replayed as a different kind of message", "To support multiple hash functions", "To allow key rotation"], 1, "A signature covers exactly the bytes hashed, so without a separator a payment authorisation may be valid as a login challenge. It adds bytes rather than saving them, and neither hash agility nor rotation is what it addresses."],
    ["k10", "ECDSA signature malleability means what, concretely?", ["A signature can be forged without the key", "(r, −s mod n) is also valid for the same message and key", "The message can be changed after signing", "Verification may fail spuriously"], 1, "A third party who can forge nothing can still alter the signature's encoding, which broke systems identifying transactions by signature hash. The message is still protected, and verification is deterministic; the fix is to normalise to low-s."],
    // k11
    ["k11", "Why is a plain SHA-256 the wrong verifier for a password?", ["It has known collisions", "It is fast, so a GPU tries billions of guesses per second", "It is not deterministic", "Its output is too short"], 1, "Speed is the design goal of a general-purpose hash and exactly the wrong property here; unsalted, it is also precomputable. SHA-256 has no practical collisions, it is deterministic, and 256 bits is plenty."],
    ["k11", "What does a salt actually accomplish?", ["It slows down each guess", "It defeats precomputation and hides the fact that two users chose the same password", "It keeps the hash secret", "It increases the password's entropy"], 1, "It makes every record its own problem, so no rainbow table is reusable and repeated hashes no longer reveal repeated passwords — at no computational cost. Slowness comes from the work factor, and a salt adds nothing to the password itself."],
    ["k11", "Why is memory hardness the specific defence against GPU cracking?", ["GPUs have slow memory buses", "Thousands of parallel cores share comparatively little fast memory, so a memory-heavy function cannot be run in thousands of instances", "GPUs cannot address more than 4 GB", "It forces the attacker to use floating point"], 1, "The attacker's advantage comes from parallelism, and memory per core is what caps it — collapsing a thousandfold advantage to something far smaller. Bus speed, address space and arithmetic type are not the constraint."],
    ["k11", "Which of these has no memory hardness at all?", ["Argon2id", "scrypt", "bcrypt", "PBKDF2"], 3, "PBKDF2 has only an iteration count, which is why it is the weakest of the four against GPUs — though it is often the only option a compliance regime permits. Argon2id and scrypt are explicitly memory-hard, and bcrypt is moderately so through its 4 KB working set."],
    ["k11", "A system pre-hashes long passwords with a raw SHA-256 digest before passing them to bcrypt. What is the bug?", ["SHA-256 is too fast", "bcrypt truncates at a NUL byte, so a digest containing zero truncates the password", "bcrypt cannot accept binary input", "The pre-hash removes the salt"], 1, "The digest must be base64-encoded first, or a password is silently cut down to whatever preceded the zero byte. The pre-hash's speed does not matter since bcrypt still runs, and the salt is applied by bcrypt itself."],
    ["k11", "What distinguishes HKDF from Argon2?", ["HKDF is newer", "HKDF is fast and takes high-entropy input; Argon2 is deliberately slow and takes a password", "HKDF produces longer keys", "Argon2 cannot be used for passwords"], 1, "They are two different jobs: running HKDF on a password gives no protection, and running Argon2 to expand a random master key wastes hundreds of milliseconds for nothing. Output length is configurable in both, and Argon2 is precisely for passwords."],
    ["k11", "What is HKDF's info parameter for?", ["Storing the salt", "Domain separation, so one master secret yields unrelated per-purpose keys", "Selecting the hash function", "Recording the key's creation time"], 1, "Distinct labels give cryptographically unrelated subkeys per purpose and direction, which is what stops one key from serving two roles. The salt is a separate parameter, and neither hash choice nor metadata belongs there."],
    ["k11", "A login endpoint returns in 1 ms for a missing user and 300 ms for a wrong password. What is the flaw?", ["The KDF is too slow", "It enumerates accounts through timing regardless of how strong the hash is", "The error message is too specific", "Nothing — the hash is still secure"], 1, "An attacker learns which addresses are registered without guessing any password, so the missing-user path must run the KDF against a dummy verifier. The KDF's cost is correct, and the messages may already be identical — the timing gives it away anyway."],
    // k12
    ["k12", "State the constant-time rule.", ["Every function must take the same wall-clock time", "No branch and no memory address may depend on secret data", "Secrets must be erased immediately after use", "All comparisons must use hardware instructions"], 1, "The requirement is about control flow and memory access patterns, not about a fixed number of nanoseconds — timing is simply how a violation is read. Erasure and hardware instructions are useful practices but neither is the rule."],
    ["k12", "Why is table-driven AES a problem on shared hardware?", ["The tables are too large for cache", "The S-box index depends on secret data, and which cache line was touched is observable", "The tables can be modified by another process", "Table lookups are slow"], 1, "A co-resident attacker evicts lines and measures refills, recovering the index and hence key bytes; AES-NI or a bitsliced implementation removes the lookup entirely. The tables are small, are not writable by others, and speed is not the security issue."],
    ["k12", "What does square-and-multiply exponentiation leak, and how is it fixed?", ["The modulus; use a larger one", "The exponent, which is the private key; use a Montgomery ladder or blinding", "The base; randomise it", "Nothing; it is already constant-time"], 1, "Multiplying only when a bit is set makes per-bit timing and power differences readable. A ladder performs the same operations either way, and blinding decorrelates whatever still leaks. The modulus is public and the base is not the secret."],
    ["k12", "Why do RSA implementations verify a signature before releasing it?", ["To catch programming errors", "Because a single faulty RSA-CRT signature factors the modulus", "To satisfy FIPS requirements", "To confirm the hash was computed correctly"], 1, "That is the Boneh–DeMillo–Lipton result, and it makes fault injection during signing a complete key recovery. Catching bugs and satisfying certification are side benefits, not the reason."],
    ["k12", "Is a flat timing benchmark evidence of constant-time code?", ["Yes, if run enough times", "No — constant time is a property of control flow and memory access, established by reading the code or with a tool", "Yes, on the same hardware as production", "Only for symmetric primitives"], 1, "Measurement noise hides small differences that an attacker sharing your cache does not have to fight, so the property has to be established structurally. Repetition and matching hardware do not change that, and it applies to all primitives."],
    ["k12", "Why does constant-time code often live in assembly or in vetted library routines?", ["Assembly is faster", "Compilers can turn branchless expressions back into branches and optimise memory scrubbing away", "High-level languages lack XOR", "Libraries are audited more often"], 1, "The source-level property is not preserved through optimisation, so the guarantee has to be established where the compiler cannot undo it. Speed is a side issue, XOR is universally available, and audit frequency is a separate argument for using libraries."],
    ["k12", "What is the relationship between a padding oracle and a side channel?", ["They are unrelated", "A padding oracle is a side channel whose signal is a distinguishable error path", "A side channel is a special case of a padding oracle", "Both require physical access"], 1, "The mechanism is identical — observable behaviour that depends on secret data — and only the channel differs. Neither is a special case of the other in the direction stated, and remote timing attacks need no physical access at all."],
    // k13
    ["k13", "What does Shor's algorithm break?", ["AES and SHA-256", "RSA, finite-field Diffie–Hellman, DSA, ECDH and ECDSA", "Only RSA", "Password hashing"], 1, "It solves factoring and discrete log in polynomial time, which covers every public-key primitive in current use. Symmetric primitives and hashes are affected by Grover instead, and much less severely."],
    ["k13", "Does doubling an RSA key length help against Shor?", ["Yes, it squares the work", "No — the cost grows polynomially in the key size", "Yes, for keys above 4096 bits", "Only in combination with a stronger hash"], 1, "Polynomial growth means a larger modulus is a rounding error to a quantum attacker, which is why the response is a different primitive rather than a bigger key. No threshold size changes this, and the hash is irrelevant."],
    ["k13", "Why is Grover's nominal halving of AES-128 not treated as a crisis?", ["Grover does not apply to block ciphers", "It parallelises only as a square root, and each of the 2⁶⁴ steps is a coherent AES evaluation", "AES-128 is already deprecated", "Quantum computers cannot evaluate AES"], 1, "A thousand machines give a speedup of about thirty-two, not a thousand, so the practical cost sits far above 2⁶⁴ — hence AES-128 remains acceptable and AES-256 is a comfortable margin. Grover does apply, and AES-128 is not deprecated."],
    ["k13", "What makes long-horizon confidentiality urgent before a quantum computer exists?", ["Standards deadlines", "Harvest now, decrypt later: traffic recorded today is decrypted when the machine arrives", "Key sizes take years to change", "Grover already applies"], 1, "The ciphertext being recorded now is the one that gets broken, so anything with a long secrecy requirement is at risk today. Compliance deadlines follow from the risk rather than causing it, and Grover is not the threat to confidentiality here."],
    ["k13", "Does forward secrecy protect recorded traffic against Shor?", ["Yes, that is exactly what it is for", "No — it protects against a compromised long-term key, not against the group becoming easy", "Yes, if the session keys were deleted", "Only with ephemeral RSA"], 1, "Shor breaks the ephemeral key agreement itself, recovering the session key from the recorded handshake. Deleting session keys does not help when they can be recomputed, and ephemeral RSA is not a thing in deployed TLS."],
    ["k13", "Which NIST standard covers post-quantum key establishment?", ["FIPS 204, ML-DSA", "FIPS 203, ML-KEM", "FIPS 205, SLH-DSA", "FIPS 140-3"], 1, "ML-KEM, from CRYSTALS-Kyber, is the key encapsulation standard published in August 2024. ML-DSA and SLH-DSA are signature standards, and FIPS 140-3 is a module validation programme rather than an algorithm."],
    ["k13", "How does a KEM's interface differ from Diffie–Hellman's?", ["It needs three round trips", "Encapsulate to a public key produces a ciphertext and a shared secret; there is no symmetric contribution of exponents", "It requires a trusted third party", "It produces a signature as well"], 1, "The shape is one-directional, so protocols built around both sides raising the other's value need restructuring rather than a primitive swap. It adds no round trips, needs no third party, and produces no signature."],
    ["k13", "Why is hybrid key agreement the current default?", ["To satisfy two different regulators", "The result holds if either component holds, and lattice assumptions are young — SIKE was broken classically in 2022 after years of analysis", "Because ML-KEM is too slow alone", "To keep handshakes small"], 1, "Combining X25519 with ML-KEM turns a surprise break of either into a bad afternoon rather than a catastrophe. ML-KEM is fast, and hybrids make handshakes larger, not smaller."],
    ["k13", "What is the main practical cost of the post-quantum migration?", ["Computation — the lattice operations are slow", "Size — kilobyte keys and signatures strain handshakes and certificate chains", "Licensing fees", "Loss of forward secrecy"], 1, "The arithmetic is often faster than the elliptic-curve operations it replaces; bandwidth and fixed-size protocol fields are what break. The standards are unencumbered, and forward secrecy is preserved."],
    // k14
    ["k14", "What does TLS 1.3's transcript signature accomplish?", ["It compresses the handshake", "It binds the key shares to a certified identity, so a substituted share is detected", "It provides forward secrecy", "It authenticates the application data"], 1, "An attacker who swaps in their own key share changes the transcript and cannot sign the changed version, which is precisely the missing piece in bare Diffie–Hellman. Forward secrecy comes from the ephemeral exponents, and application data is protected by the record layer."],
    ["k14", "Why is TLS 1.3 0-RTT early data replayable?", ["The key is weak", "Nothing in the client's first flight makes it fresh", "The server does not authenticate it", "It is sent unencrypted"], 1, "It is derived from a previous session with no server contribution to establish freshness, so the specification limits it to idempotent requests. It is encrypted and authenticated; freshness is the missing property, not confidentiality."],
    ["k14", "What did DROWN demonstrate about disabling old protocols?", ["Old protocols are safe if rarely used", "A server still speaking SSLv2 anywhere with the same key was enough to attack its TLS sessions", "Downgrade attacks require an active attacker on both connections", "SSLv2 was never widely deployed"], 1, "Sharing a key across a weak endpoint is enough, so weak options must be removed rather than deprioritised or moved to another port. Rarity of use is no defence, and the attack does not need both connections."],
    ["k14", "Authentication proves who produced a message. What does it not prove?", ["That the message is well-formed", "That it is fresh, or that you have not already acted on it", "That the sender holds the key", "That the content is confidential"], 1, "A captured valid message replayed later is still valid and nothing was forged, so freshness must be constructed with a challenge, a sequence number, a bounded timestamp window, or a recorded one-time token. Confidentiality is a separate property that authentication never claimed."],
    ["k14", "An encrypted database cell is moved from one row to another and decrypts perfectly. What was missing?", ["A stronger cipher", "Context binding — the row identifier as associated data", "A longer key", "A unique IV"], 1, "Nothing in the ciphertext said where it belonged, so it stays valid anywhere. A stronger cipher and a longer key change nothing about placement, and a unique IV prevents keystream reuse rather than relocation."],
    ["k14", "Why must key rotation be designed in from the beginning?", ["Because keys expire automatically", "Because without a key identifier travelling with each ciphertext and a decryption path for the previous key, rotation is never possible in practice", "Because standards require annual rotation", "Because old keys become computationally weaker"], 1, "Retrofitting a way to tell which key encrypted which record onto a live corpus is what makes rotation never happen. Keys do not expire on their own, and they do not weaken with age — the environment around them does."],
    ["k14", "An account is protected by strong encryption and a support-desk password reset. What is it protected by?", ["The encryption", "The support desk's identity verification procedure", "Both equally", "The key length"], 1, "The recovery flow is part of the threat model, and an attacker will use the cheapest path — which is a phone call, not cryptanalysis. The cipher is irrelevant to that path, and key length even more so."],
    ["k14", "What makes cryptographic bugs survive code review more reliably than ordinary ones?", ["They are usually in third-party libraries", "The code works: it encrypts, decrypts and passes its tests, and fails only against an adversary who is looking", "They are always in assembly", "Reviewers lack access to the key material"], 1, "There is no failing happy path to notice, so reading a cryptographic design means asking what the attacker is allowed to do rather than whether the normal case is correct. Library code is generally better than hand-written, and neither language nor key access is the obstacle."]
];
//# sourceMappingURL=questions.js.map