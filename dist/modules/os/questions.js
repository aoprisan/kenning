/** [moduleId, text, options, correctIndex, explanation] */
export const questions = [
    // o1
    ["o1", "Why can the isolation between processes not be enforced in software alone?", ["Software is too slow", "A process could jump into the enforcing code, rewrite its tables, or refuse to yield", "Compilers cannot generate safe code", "Memory is shared by definition"], 1, "Software cannot restrain software, which is why the enforcement lives in the processor's mode bit. Speed is not the objection, compilers do not enter into it for arbitrary binaries, and memory sharing is a consequence of the lack of enforcement rather than its cause."],
    ["o1", "Why does a system call pass a number rather than an address to jump to?", ["Numbers are smaller", "A number is data checked against a table; an address would let the caller choose where to enter the kernel", "To support 32-bit systems", "Because registers cannot hold addresses"], 1, "The kernel installed the trap handler's address at boot in a register user mode cannot write, so the caller says what it wants and never where to go. That is the whole safety argument. Size, word width and register capacity are irrelevant."],
    ["o1", "Why must system call arguments be copied rather than used in place?", ["Copying is faster than sharing", "A user pointer is an untrusted integer that may be unmapped, may point into the kernel, and may change between the check and the use", "The kernel uses a different pointer size", "To keep the cache warm"], 1, "All three hazards are real and the third is a race, which is why kernels use dedicated accessors and why forgetting one is a classic vulnerability. Copying is slower, not faster, and pointer size and caching are not the issue."],
    ["o1", "Roughly what is the cost ratio between a system call and a plain function call?", ["About the same", "Roughly 10×", "Roughly 100× or more", "Roughly 2×"], 2, "A function call is about a nanosecond and a syscall hundreds, before counting the indirect cost of colder caches and TLB after it returns. The ratio, not the absolute figure, is what drives every design that batches across the boundary."],
    ["o1", "What is the indirect cost of a system call?", ["The time spent validating arguments", "The colder caches, branch predictors and TLB that the program finds after it returns", "The time to schedule the process again", "The memory used by the kernel stack"], 1, "The kernel runs with its own working set, so the cost lands after the call returns and is invisible to a measurement bracketing the call itself. Argument validation is part of the direct cost, and no rescheduling is required for a syscall."],
    ["o1", "What do buffered I/O, the vDSO and io_uring have in common?", ["They are all kernel modules", "They all amortise or avoid crossing the user–kernel boundary", "They all use shared memory", "They all require root"], 1, "Buffering turns many writes into one, the vDSO removes the trap for reads of kernel-maintained data, and io_uring batches submissions through shared rings. Only the last uses shared memory, none is a module, and none needs root."],
    ["o1", "What does a microkernel trade for its isolation?", ["Memory footprint", "A filesystem-to-driver call becomes an IPC round trip rather than a function call", "The ability to run device drivers", "Support for virtual memory"], 1, "That performance cost is why monolithic kernels won in practice, despite the microkernel argument about isolation being correct — and it is why a verified microkernel is the only kind anyone has formally verified. Drivers and virtual memory both work fine."],
    ["o1", "What does \"separate mechanism from policy\" mean concretely in a kernel?", ["Drivers should be in user space", "The kernel provides context switching; a replaceable scheduler decides who runs next", "Policies must be configurable at boot", "Security should be separate from performance"], 1, "Linux has replaced its scheduling policy wholesale several times without changing the switching mechanism, which is the principle working. Driver placement is the microkernel argument, and boot configuration is not the same as replaceability."],
    // o2
    ["o2", "A machine shows 5% CPU utilisation and everything is slow. What does that suggest?", ["It needs more cores", "Tasks are blocked on something that is not the CPU", "The scheduler is misconfigured", "Memory is fragmented"], 1, "Ready means short of CPU and blocked means short of something else — usually storage or a lock — and adding cores helps only the first. That is also why the CPU number alone is such a poor diagnostic."],
    ["o2", "Why can Linux's load average be high on a machine that looks idle?", ["It counts threads rather than processes", "It includes tasks in uninterruptible sleep, typically waiting on storage", "It is averaged over too long a window", "It counts zombies"], 1, "Unlike most Unixes, Linux's figure includes uninterruptible-sleep tasks, so storage waits inflate it — which is why a high load average with low CPU usually means disk. Zombies consume nothing and are not counted."],
    ["o2", "Why are fork and exec separate calls?", ["To support processes without programs", "So the child can adjust redirections, descriptors and privileges between them, using ordinary code", "Because exec cannot allocate memory", "For backwards compatibility with older Unixes"], 1, "That window is where a shell sets up a pipeline, and it replaces what would otherwise be a hundred flags on a single spawn call. It is a deliberate design choice, not a compatibility artefact."],
    ["o2", "A large process forks and both sides continue running. When is memory actually copied?", ["Immediately, at fork time", "On the first write to each shared page", "When exec is called", "Never — the pages stay shared"], 1, "Copy-on-write duplicates only the page tables at fork time and copies a page when either side writes it — which is why a garbage collector or a refcount in an object header can un-share an entire heap that was meant to stay shared."],
    ["o2", "Why is fork() dangerous in a threaded program?", ["The child gets too little memory", "Only the calling thread exists in the child, and locks held by the others are still held by nobody", "File descriptors are not inherited", "Signals are lost"], 1, "If one of those was the allocator's lock, the child deadlocks on its first malloc — which is why only async-signal-safe calls are legal before exec, and why posix_spawn exists. Descriptors are inherited normally."],
    ["o2", "What is the largest cost of a context switch?", ["Saving and restoring registers", "The cache and TLB reload paid by the next timeslice", "Switching page tables", "Updating the scheduler's data structures"], 1, "The direct cost is a few microseconds; the indirect cost is a working set fetched from memory afterwards, charged to the next timeslice and invisible to any measurement of the switch. That is the whole argument for thread pools sized to cores."],
    ["o2", "Ten thousand threads on eight cores. What actually happens?", ["Ten thousand things run concurrently", "Eight things run, and the machine spends significant effort deciding which eight", "The scheduler rejects the extra threads", "Each thread gets 1/10,000 of a core continuously"], 1, "Parallelism is bounded by cores, and every switch discards the cache state the previous thread had just built. The kernel will happily create them; it is the throughput that suffers."],
    ["o2", "What distinguishes a voluntary from an involuntary context switch, and why does it matter?", ["Voluntary ones are faster", "Voluntary means the task blocked; involuntary means it was preempted — and the two have opposite remedies", "Voluntary switches happen only in user space", "Involuntary switches indicate a bug"], 1, "A high voluntary count means find what it is waiting on; a high involuntary count means the machine is oversubscribed. Most monitoring reports only the sum, which cannot distinguish them."],
    ["o2", "What is a zombie process consuming?", ["Its full memory allocation", "A few hundred bytes holding an exit status nobody has read", "One CPU core", "An open file descriptor per child"], 1, "Thousands of zombies are a symptom rather than a leak: the parent is not calling wait(). It is the usual outcome when a process not written to be PID 1 becomes PID 1 in a container."],
    // o3
    ["o3", "Ten one-second jobs queue behind one hundred-second job under FIFO. What is this called?", ["Starvation", "The convoy effect", "Priority inversion", "Thrashing"], 1, "Nothing is wrong except the order, and average turnaround is roughly ten times worse than if the long job ran last. Starvation is never being run at all, priority inversion involves a lock, and thrashing is a memory phenomenon."],
    ["o3", "Why is shortest-job-first not used as stated?", ["It is not optimal", "It requires knowing job lengths in advance, and it starves long jobs", "It cannot be preempted", "It needs too much memory"], 1, "SJF is provably optimal for average turnaround when jobs arrive together, and both problems are practical rather than theoretical — which is exactly what MLFQ works around by inferring length from behaviour."],
    ["o3", "What does round robin deliberately sacrifice?", ["Response time", "Turnaround — it finishes everything late rather than anything early", "Fairness", "CPU utilisation"], 1, "Interleaving is what buys the excellent response time, and it means no job completes early. The quantum is the whole tuning knob between responsiveness and switch overhead."],
    ["o3", "In MLFQ, what does dropping a job a level when it uses its whole quantum accomplish?", ["It punishes badly written programs", "It infers that the job is CPU-bound rather than interactive", "It prevents starvation", "It reduces context switches"], 1, "Interactive jobs block before their quantum expires and stay high; CPU-bound jobs sink and get long quanta there, which suits them. Starvation is what the periodic boost addresses, and this rule increases switching rather than reducing it."],
    ["o3", "How does a task game MLFQ, and what fixes it?", ["By forking repeatedly; limit process counts", "By issuing a pointless I/O just before each quantum expires; account CPU time across quanta rather than per quantum", "By raising its own nice value; restrict nice", "By spinning; detect spin loops"], 1, "Never using a full quantum means never being demoted, so priority stays at the top forever. Accounting the total used across quanta makes the sum demote it regardless of how the time was broken up."],
    ["o3", "What is nice in Linux's fair-share scheduler?", ["A queue level", "A weight on the rate at which a task accrues virtual runtime", "A fixed share percentage", "A real-time priority"], 1, "CFS and EEVDF run whichever task has received least CPU, tracked as virtual runtime, and nice changes how quickly a task accrues it — so it is a weight, not a queue. Real-time priorities are a separate scheduling class."],
    ["o3", "Three tasks and one lock: LOW holds it, HIGH needs it, MED is runnable and never touches it. What happens?", ["HIGH runs after LOW finishes", "MED preempts LOW, so LOW never releases the lock, and HIGH is blocked indefinitely by a task it shares nothing with", "The kernel detects a deadlock and aborts one", "HIGH's priority is lowered"], 1, "That is priority inversion, and it is what repeatedly reset Mars Pathfinder in 1997. It is not a deadlock — no cycle exists — and it is fixed by priority inheritance, which raises LOW until it releases."],
    ["o3", "Why might a scheduler leave a core idle while a task is queued elsewhere?", ["A bug in the load balancer", "Migrating the task would destroy its cache affinity, and possibly cross a NUMA boundary", "The core is reserved for the kernel", "Power management prevents it"], 1, "A migrated task finds nothing in the new core's cache and pays a memory round trip for its whole working set, so Linux migrates reluctantly in a hierarchy matching the machine's topology. It can be correct behaviour rather than a defect."],
    // o4
    ["o4", "Where do the nine index bits per page-table level on x86-64 come from?", ["They were chosen for convenience", "A 4 KiB table of 8-byte entries holds 512 = 2⁹ entries", "The TLB has 512 entries", "It matches the cache line size"], 1, "The geometry falls out of the page size: 4096 ÷ 8 = 512 entries, so nine index bits, and four levels of nine plus twelve offset bits gives exactly 48. Nothing about it is arbitrary."],
    ["o4", "Why is the page table a tree rather than a flat array?", ["Trees are faster to search", "A flat 48-bit table would need 512 GiB per process, almost all of it describing unmapped space", "To support variable page sizes", "To allow sharing between processes"], 1, "A null entry at the top level elides an entire 512 GiB subtree, so a process using a few megabytes needs a handful of table pages. A flat array would actually be faster to index — it is the space that is impossible."],
    ["o4", "Which page table bits does the hardware set rather than the kernel?", ["Present and writable", "Accessed and dirty", "User and NX", "The frame number"], 1, "That single piece of hardware cooperation is what makes practical page replacement possible: the kernel clears them and reads them back to learn what was used and what must be written out. The others are set by the kernel."],
    ["o4", "A program walks a 100 MB hash table at random and is slow, with excellent cache hit rates. What is the likely cause?", ["Branch misprediction", "TLB misses — the working set exceeds the TLB's roughly 6 MB reach", "False sharing", "Page faults"], 1, "Translation caching is separate from data caching, so a cache-hit-rate metric cannot show this. Huge pages are the lever, because they multiply reach by 512."],
    ["o4", "What do huge pages buy and what do they cost?", ["More memory; nothing", "512× the TLB reach; internal fragmentation and coarser granularity for the protection flags", "Faster memory access; more page faults", "Better security; slower context switches"], 1, "One entry covers 2 MiB instead of 4 KiB. Transparent huge pages add a further cost — compacting memory to form a contiguous region can stall the faulting process, which is why databases often disable them."],
    ["o4", "Why are munmap and mprotect expensive on a large, heavily threaded machine?", ["They must zero the pages", "Changing a mapping requires a TLB shootdown IPI to every core that might hold the stale translation", "They take a global kernel lock", "They flush the page cache"], 1, "The TLB is not coherent across cores, so the initiating core interrupts the others and waits — a cost that scales with core count. Zeroing happens on allocation, not on unmapping."],
    ["o4", "Why is a null pointer dereference a clean segmentation fault?", ["The compiler inserts a check", "Page zero is deliberately left unmapped, so any access to it faults", "The CPU special-cases address zero", "The allocator reserves it"], 1, "It is a deliberate layout decision rather than a hardware or compiler feature, and it is what turns a whole class of bug into an immediate, diagnosable fault instead of a corrupted read."],
    ["o4", "Two processes map the same library. What makes that cheap?", ["The library is compiled twice", "Two page table entries can hold the same physical frame, so read-only text exists once in memory", "The loader compresses it", "Each process gets a copy-on-write copy immediately"], 1, "The same mechanism gives copy-on-write fork, shared memory and the page cache. Writable data is copy-on-write, but the text is genuinely shared."],
    // o5
    ["o5", "What is a page fault?", ["An error indicating corrupted memory", "A control transfer that lets the kernel construct a mapping and restart the instruction", "A signal delivered to the process", "A hardware failure"], 1, "Nothing has gone wrong — the mechanism is how demand paging, copy-on-write, mmap and stack growth all work, and the program never learns it happened."],
    ["o5", "What is the difference that matters between a minor and a major page fault?", ["Minor faults are in user space", "A minor fault is resolved in memory in microseconds; a major fault reaches storage and costs four to five orders of magnitude more", "Major faults kill the process", "Minor faults do not update the page table"], 1, "Millions of minor faults are routine and mean nothing is wrong. A rising major fault rate is the clearest early signal that a machine is short of memory."],
    ["o5", "Why can LRU not be implemented exactly?", ["It requires knowing the future", "It would need a timestamp update on every single memory access", "It cannot handle shared pages", "The hardware provides no reference bit"], 1, "Knowing the future is OPT's problem, not LRU's. CLOCK approximates LRU using the hardware-set accessed bit with no per-access work, which is what real kernels run."],
    ["o5", "Bélády's anomaly is that", ["LRU can perform worse than FIFO", "FIFO can take more faults with more frames", "OPT is not actually optimal", "more frames always help"], 1, "LRU and OPT are stack algorithms — the pages held with n frames are always a subset of those held with n+1 — and provably cannot show it. FIFO has no such containment property."],
    ["o5", "A machine is thrashing. What does CPU utilisation look like?", ["Pinned at 100%", "Low, because every process is blocked waiting on storage", "Oscillating rapidly", "Normal"], 1, "That is what makes it so deceptive: a dashboard watching CPU shows an idle machine while nothing completes. Major fault rate, or pressure-stall information, is what reveals it."],
    ["o5", "Why can a machine with no swap perform worse under memory pressure than one with swap?", ["Swap is faster than memory", "Without swap the kernel cannot evict cold anonymous pages, so it evicts file-backed pages instead and takes major faults on executable code", "The OOM killer runs sooner", "Page tables grow larger"], 1, "The kernel's choices are narrowed to pages it can re-read from a file, so hot code and cache are evicted while cold anonymous data stays. What swap does not do is rescue a machine whose working set genuinely exceeds memory."],
    ["o5", "malloc returns non-null and the program is killed later while writing to that memory. Why?", ["The pointer was invalid", "Overcommit — pages are attached on first touch, so the allocation was a promise the kernel could not keep", "The allocator was corrupted", "A signal handler failed"], 1, "Nobody called anything at the moment the promise failed, so there is no error to return and the kernel picks a process to kill. Overcommit is what makes copy-on-write fork and large sparse mappings cheap."],
    ["o5", "A container restarts with no error in the application log. What should you check first?", ["The application's exception handler", "Exit code 137 and the host's dmesg for an OOM kill", "The network configuration", "The container image build"], 1, "An OOM kill at a cgroup limit gives the application no chance to log anything. The evidence is outside the container, which is why looking in its own output wastes so much time."],
    // o6
    ["o6", "What is the difference between internal and external fragmentation?", ["Internal is in the kernel, external in user space", "Internal is surplus inside an allocation; external is enough total free memory with no single run large enough", "Internal is permanent, external is temporary", "They are two names for the same thing"], 1, "Every allocator design trades one against the other: size classes create internal fragmentation and mostly eliminate external, while exact-fit does the reverse."],
    ["o6", "Why does Linux allocate physical page frames with a buddy system?", ["It minimises internal fragmentation", "A block's buddy is one address bit away, so coalescing is O(1) with no search", "It supports variable page sizes", "It is the simplest possible allocator"], 1, "Finding the neighbour to merge with requires flipping one bit rather than searching, which is what makes it fast. Internal fragmentation is up to 2× in the worst case, so it is not minimising that."],
    ["o6", "A program frees 100 MB and its RSS does not fall. What is happening?", ["A memory leak", "The allocator is keeping the region for reuse, because returning it costs a syscall and getting it back costs a fault per page", "The kernel has not updated the accounting", "The memory was never actually allocated"], 1, "This is correct and desirable behaviour — a program allocating and freeing in a loop would otherwise spend its life crossing the kernel boundary. Large single allocations served by their own mmap are unmapped on free, which is why the behaviour seems inconsistent."],
    ["o6", "How do you distinguish a leak from fragmentation?", ["By watching RSS over time", "By comparing the allocator's own live-bytes accounting against RSS — a leak grows both, fragmentation grows only RSS", "By counting mallocs and frees", "You cannot"], 1, "RSS alone cannot separate a leak, fragmentation and deliberate retention. malloc_info, jemalloc statistics or a heap profiler give the live-bytes figure that does."],
    ["o6", "Why do modern allocators give each thread a private cache?", ["To improve locality", "To avoid a global lock that would serialise every allocation in the program", "To reduce internal fragmentation", "To support thread-local storage"], 1, "A single locked free list is a hard scaling ceiling on a many-core machine. The cost is that memory in flight rises with thread count, which is why the same program's footprint differs between allocators."],
    ["o6", "What is the pathological pattern for a thread-caching allocator?", ["Many small allocations", "Allocating on one thread and freeing on another, repeatedly", "Allocating very large blocks", "Freeing in reverse order"], 1, "The memory has to cross caches every time, which producer–consumer designs do by construction. It is worth recognising before concluding that the allocator is simply slow."],
    ["o6", "What does an arena allocator give up, and what does it get?", ["Gives up speed, gets safety", "Gives up freeing individual objects, gets allocation in a few cycles with no fragmentation or bookkeeping", "Gives up thread safety, gets locality", "Gives up memory, gets simplicity"], 1, "Allocation is a pointer bump and freeing is a reset, which fits any workload with a natural lifetime boundary — a request, a compiler pass, a frame. Where it fits it removes the problem rather than optimising it."],
    ["o6", "Why can a large physically-contiguous kernel allocation fail on a machine with plenty of free memory?", ["The kernel reserves memory for itself", "Paging removes the need for contiguity in general, but DMA buffers genuinely need it, and physical memory can be externally fragmented", "The buddy allocator has a size limit", "Free memory is reserved for the page cache"], 1, "Virtual memory hides physical fragmentation from ordinary allocations, so it goes unnoticed until something needs real contiguous frames. Compaction exists to address exactly this."],
    // o7
    ["o7", "Why is a shared counter increment unsafe without synchronisation?", ["Addition is not atomic on modern CPUs", "It compiles to load, add and store, so another thread can load the old value in between", "The compiler reorders it", "The value may exceed the register width"], 1, "The window is three instructions wide, which makes the failure rare enough to pass every test and to appear under production load. Even a single atomic add instruction would be safe; the problem is the sequence."],
    ["o7", "What is the difference between a data race and a race condition?", ["They are the same thing", "A data race is unsynchronised concurrent access with a write, and is detectable by tools; a race condition is an outcome depending on timing, and is a design property", "A data race involves data, a race condition involves control flow", "A race condition is always a bug, a data race is not"], 1, "ThreadSanitizer finds data races; nothing finds race conditions in general. Check-then-act is the classic race condition that stays broken however carefully each half is locked."],
    ["o7", "In C, C++ and Go, what is a data race?", ["An unpredictable but bounded result", "Undefined behaviour — no constraint at all on what the program does", "A performance problem", "A warning the compiler emits"], 1, "It is not \"you get one value or the other\": the compiler is entitled to assume it does not happen, and optimises accordingly. That is why a racy spin loop can be compiled into an infinite one."],
    ["o7", "Two threads: A does x=1 then reads y; B does y=1 then reads x. Both read 0. Is this possible?", ["No, no interleaving allows it", "Yes, on real x86 — store buffers let each store sit before becoming visible", "Only on ARM and POWER", "Only with compiler optimisation enabled"], 1, "No interleaving permits it, and the hardware does it anyway, because sequential consistency is not what processors implement. x86 is one of the stronger models; ARM and POWER reorder considerably more."],
    ["o7", "\"It compiles to a single instruction, so it is atomic.\" What is wrong with this?", ["Single instructions are not atomic", "Atomicity and ordering are different properties — being indivisible says nothing about when other threads see it", "Instructions can be interrupted", "The compiler may split it later"], 1, "An aligned single-instruction store is genuinely atomic in the sense that nobody sees half of it, and that gives no guarantee about visibility or about its order relative to your other stores."],
    ["o7", "What does volatile do in C and C++?", ["Provides acquire-release semantics", "Stops the compiler caching the value in a register, and constrains the processor not at all", "Makes the access atomic", "The same thing it does in Java"], 1, "It was designed for memory-mapped device registers and is useless for cross-thread synchronisation in C and C++. Java's volatile genuinely does carry acquire-release semantics, which is exactly why the confusion persists."],
    ["o7", "What guarantee does a modern language memory model actually give?", ["All memory operations appear in program order", "A program free of data races behaves as if sequentially consistent", "Atomic operations are always sequentially consistent", "Reads never see stale values"], 1, "Synchronise properly and you may reason with interleavings; race, and you get nothing at all. Atomics have selectable orderings, and stale reads are permitted between synchronisation points."],
    ["o7", "What is the ABA problem in a compare-and-swap loop?", ["The CAS may fail spuriously", "A value read as A, changed to B and back to A passes the comparison while the state it described has changed", "Two threads can both succeed", "The loop may never terminate"], 1, "Pointer-based lock-free structures handle it with tagged pointers, hazard pointers or epoch reclamation — and its existence is the main reason hand-rolled lock-free code is rarely the right first move."],
    // o8
    ["o8", "Why is an uncontended mutex acquisition so cheap?", ["The kernel fast-paths it", "It is a compare-and-swap on a word in user memory — no syscall at all", "Locks are cached in registers", "The scheduler avoids preempting lock holders"], 1, "A futex only enters the kernel when the CAS fails and the thread must block. That is two orders of magnitude between the two paths, which is why \"locks are slow\" is a statement about contention."],
    ["o8", "When is spinning rather than blocking the right choice?", ["Always, since blocking needs a syscall", "When the expected wait is shorter than a context switch and the holder is actually running on another core", "When there are many waiters", "In user space, but not in the kernel"], 1, "It is correct in a kernel holding a lock for a few instructions and wrong in user space where the holder can be descheduled while you burn a timeslice. Adaptive mutexes spin briefly then block."],
    ["o8", "A program is 95% parallel. What is its maximum speedup on infinite cores?", ["95×", "20×", "100×", "Unbounded"], 1, "Amdahl: 1/(1−p) = 1/0.05 = 20. And that is the optimistic model, because it assumes coordination between the cores is free."],
    ["o8", "Measured throughput rises, peaks at twenty threads and declines at forty. What does that indicate?", ["A bug in the benchmark", "The coherence term of the Universal Scalability Law — cache-line traffic growing as N²", "The serial fraction is 5%", "Memory bandwidth saturation"], 1, "Amdahl's law produces a curve that flattens; only a coherence cost makes it turn over and decline. The fix is to reduce sharing, not to add hardware."],
    ["o8", "What is the practical way to prevent deadlock?", ["Detect cycles and abort a victim", "Define a global lock order and always acquire in it, which makes a circular wait impossible", "Use trylock everywhere", "Use a single global lock"], 1, "Breaking any one of the four conditions suffices, and attacking circular wait is the cheapest. Detection is what databases do because they can abort a transaction; trylock introduces livelock without backoff."],
    ["o8", "What single rule prevents most real deadlocks?", ["Always use recursive mutexes", "Never block on anything unbounded while holding a lock", "Acquire locks in alphabetical order", "Limit the number of locks"], 1, "Real deadlocks usually involve a lock plus something else that waits — a full queue, a network call, an allocation that triggers reclaim — rather than two threads and two mutexes."],
    ["o8", "What is convoying?", ["Threads acquiring locks in the same order", "A lock holder being descheduled, so every waiter blocks on a lock whose owner is not running", "Many threads waking at once for one resource", "A queue of I/O requests"], 1, "Throughput then collapses well below what the critical section's length predicts, which is the specific reason not to hold a lock across I/O, an allocation that may fault, or a syscall. The third option describes a thundering herd."],
    ["o8", "What does RCU give that a reader-writer lock does not?", ["Writer priority", "Readers take no lock at all, so reads add no coherence traffic", "Deadlock freedom", "Support for multiple writers"], 1, "A reader-writer lock's own bookkeeping is a shared write, so readers still contend. RCU's cost is complexity and deferred reclamation, which is why it lives inside kernels rather than in application code."],
    // o9
    ["o9", "Why must wait() release the mutex and enqueue the thread atomically?", ["To avoid a lost wakeup — otherwise a signal can land in the gap and never be seen", "For performance", "To satisfy the POSIX specification", "To allow multiple waiters"], 0, "If the two were separable, another thread could take the mutex in between, change the state, and signal a condition variable this thread has not yet joined. Both sides then wait forever, which is why you cannot build a correct condition variable from a mutex and a sleep."],
    ["o9", "Why must you loop on the predicate rather than using an if?", ["To handle multiple threads", "Mesa semantics, spurious wakeups and broadcast each independently make a wake carry no guarantee", "Because wait() can fail", "To avoid holding the lock too long"], 1, "Any one of the three is sufficient on its own. The reliable formulation is that a return from wait tells you nothing except that you hold the mutex again."],
    ["o9", "What are Mesa semantics?", ["The signaller hands the lock directly to the waiter", "A signal is a hint: the waiter becomes runnable and must reacquire the mutex, by which time the state may have changed", "Signals are queued and delivered in order", "Only one waiter may exist per condition variable"], 1, "The alternative — Hoare semantics, described in the first option — is cleaner to reason about and essentially unimplemented, because it requires an immediate context switch on every signal."],
    ["o9", "POSIX permits pthread_cond_wait to return without any signal. Why?", ["It is a specification defect", "It is a deliberate allowance that makes implementations simpler and faster", "Signals can be lost on some platforms", "To support timeouts"], 1, "Spurious wakeups are permitted by design, which is one of the three independent reasons the predicate must be rechecked in a loop."],
    ["o9", "One condition variable serves both \"not empty\" and \"not full\". What can go wrong with signal()?", ["Nothing, if the predicate is checked in a loop", "The woken thread may be one that cannot proceed, while the one that could stays asleep — the system stalls with work available", "The signal may be delivered twice", "Waiters are woken in the wrong order"], 1, "Looping keeps it correct but not live: the wrong thread rechecks, fails and sleeps again. Use one condition variable per predicate, or broadcast and pay the thundering herd."],
    ["o9", "What can a counting semaphore express that a mutex cannot?", ["Mutual exclusion", "A bound of N concurrent holders — a connection pool or a rate limit", "Recursive acquisition", "Priority inheritance"], 1, "Initialised to 1 it is a mutex and to 0 a signalling latch; the counting case is the one with no mutex equivalent. Priority inheritance is something a semaphore specifically cannot support."],
    ["o9", "Why can a semaphore not support priority inheritance?", ["It is implemented in user space", "It has no owner — any thread may post one another waits on", "It does not block", "It is not part of POSIX"], 1, "Having no owner is what makes it a signalling device, and it also means nothing can detect a thread releasing something it never acquired. A mutex knows its holder and can therefore boost it."],
    ["o9", "In a bounded buffer, why must the counting semaphore be acquired before the mutex?", ["It is faster that way", "Reversing them is a textbook deadlock: a thread holds the mutex while waiting for a slot only the mutex holder could free", "The semaphore is slower to acquire", "To keep the buffer ordered"], 1, "It is the lock-ordering rule of the previous module in its most cited instance. Speed and ordering of the data are unrelated to the requirement."],
    // o10
    ["o10", "What is a hard link?", ["A copy of the file", "A second, equally real name for the same inode", "A pointer to another file's path", "A directory entry with special permissions"], 1, "Neither name is the original, and the data survives until the link count reaches zero and no descriptor still refers to it. The third option describes a symlink, which is a different mechanism entirely."],
    ["o10", "df shows a full disk and du finds nothing. What is the usual cause?", ["Filesystem corruption", "A deleted file that a process still has open, so its blocks are not freed", "Reserved blocks for root", "Sparse files"], 1, "The inode survives until the last descriptor closes, which is why lsof +L1 is the diagnostic. Reserved blocks are a fixed percentage and would not appear suddenly."],
    ["o10", "After fork, parent and child write to an inherited descriptor. What do they share?", ["Nothing — each has its own", "The open file description, and therefore the file offset", "Only the inode", "A copy-on-write copy of the buffer"], 1, "Two separate open() calls give independent offsets; dup and fork share one. That is why O_APPEND exists — it makes seek-and-write atomic in the kernel for concurrent appenders."],
    ["o10", "What does a successful write() guarantee?", ["The data is on the device", "The kernel has the data in the page cache and has taken responsibility for it", "The data is in the drive's cache", "The data will be written within a second"], 1, "Nothing has reached the device, and a power cut loses it with success already returned. Making it durable requires fsync, which is the whole of the next module."],
    ["o10", "Why is \"free memory\" a misleading metric on Linux?", ["It excludes kernel memory", "The kernel uses otherwise-idle memory for the page cache and reclaims it on demand — read available instead", "It is measured in different units", "It includes swap"], 1, "A machine with almost no free memory and a large cache is working correctly, because unused memory is wasted memory. The available figure accounts for what can be reclaimed."],
    ["o10", "What makes mmap genuinely zero-copy?", ["It uses DMA", "The page cache pages are your pages, so there is no cache-to-user copy at all", "It bypasses the filesystem", "It compresses the data"], 1, "A buffered read copies from the cache into your buffer; a mapping has nothing to copy. For a file read by many processes that is one physical copy in memory rather than one per process."],
    ["o10", "A partition reports free space and refuses to create a file. What should you check?", ["Permissions", "df -i — inodes are allocated at format time and can be exhausted independently of space", "The mount options", "Disk quotas"], 1, "A directory tree of millions of tiny files is the classic way to reach it on ext-family filesystems, and it is not the check anyone thinks to run first."],
    ["o10", "Why prefer openat and friends over path-based calls in security-sensitive code?", ["They are faster", "Path lookup is not atomic, so a checked path and an opened path may not be the same object", "They support longer paths", "They avoid the page cache"], 1, "That is the check-then-act race applied to the filesystem, and acting on descriptors relative to a directory removes the window. Path length and caching are unrelated."],
    // o11
    ["o11", "What is the difference between fsync and fdatasync?", ["fdatasync is asynchronous", "fdatasync skips metadata not needed to read the data back, such as a timestamp update, saving an extra write", "fsync applies to the whole filesystem", "fdatasync does not flush the device cache"], 1, "Both are synchronous and both flush the device. Skipping an inode write for a mtime update can halve the I/O for a database's data files, which is why it is the usual choice there."],
    ["o11", "Which crash outcome when appending a block is corruption rather than loss?", ["The data block is written but the inode is not", "The inode is updated but the free-space bitmap is not, so another file can be given the same block", "The bitmap is updated but nothing else", "All of them are equally bad"], 1, "Losing the write is survivable and a leaked block is harmless; two files sharing one block is silent corruption. That is the case journaling exists to prevent."],
    ["o11", "What makes the commit record the centre of a journaling design?", ["It contains a checksum", "It is small enough to be written atomically, so it is the single point at which the operation goes from not-happening to will-happen", "It is written last", "It identifies the transaction"], 1, "There is no intermediate state a crash can expose: either the commit record is there and the journal is replayed, or it is not and the entry is discarded."],
    ["o11", "What does ext4's data=writeback risk that data=ordered does not?", ["Slower recovery", "Metadata can reference blocks whose data was never written, exposing whatever those blocks previously held", "Lost metadata updates", "Longer fsck times"], 1, "data=ordered writes the data before the metadata that references it, which closes exactly that window. It is the default precisely because the alternative can expose another file's old contents."],
    ["o11", "You create a file and fsync it. A crash follows. What can be missing?", ["The file's contents", "The file's name — the directory entry is metadata in the parent and needs its own fsync", "The file's permissions", "Nothing"], 1, "A crash can leave a file with durable contents and no name at all, which is why the atomic-replace idiom ends with an fsync of the directory."],
    ["o11", "fsync returns EIO. What is the correct response?", ["Retry until it succeeds", "Treat it as unrecoverable and stop — Linux historically cleared the error and marked the pages clean, so a retry could succeed with the data gone", "Fall back to sync()", "Reopen the file and rewrite"], 1, "That was fsyncgate, found by PostgreSQL in 2018. The kernel now reports the error to every descriptor open at the time, and the application-level lesson stands regardless."],
    ["o11", "Why does the atomic-replace idiom fsync the temporary file before renaming it?", ["To flush the directory", "Otherwise the rename can be durable while the contents are not, leaving a correctly-named truncated file", "To update the modification time", "To avoid a partial rename"], 1, "rename is atomic with respect to observers by specification, so a reader never sees a partial file — but atomicity is not durability, and that gap is what lost a generation of editors' files on power loss."],
    ["o11", "What is a torn write?", ["A write interrupted by a signal", "A write larger than the device's atomic unit interrupted midway, leaving a block half old and half new", "A write to a bad sector", "A write split across two files"], 1, "It is why databases checksum pages and why PostgreSQL writes full pages to its WAL after a checkpoint. Signals and bad sectors produce different, detectable failures."],
    // o12
    ["o12", "Roughly how much slower is a main memory reference than an L1 cache reference?", ["About 10×", "About 100×", "About 1000×", "About the same"], 1, "Roughly 1 ns against roughly 100 ns, which is why data layout and cache-friendly access so often beat algorithmic cleverness at small scales."],
    ["o12", "Why has polling returned for NVMe devices?", ["Interrupts are unreliable", "The device can respond faster than an interrupt can be delivered, so the interrupt's own latency dominates", "It uses less power", "DMA requires it"], 1, "Polling burns a CPU and has the lowest possible latency, which becomes the right trade once the wait is only a few microseconds. Interrupts remain correct, just relatively expensive here."],
    ["o12", "Why are interrupt handlers split into a top and bottom half?", ["To support multiple devices", "Long work in the top half runs with interrupts disabled and delays every other interrupt on the machine", "To allow nesting", "To separate kernel from user code"], 1, "The symptom is system-wide latency jitter with no process to blame, which is why the top half only acknowledges the device and defers the real work."],
    ["o12", "Why can flash not overwrite a page in place?", ["Pages are read-only after the first write", "Writes act on pages but erases act on much larger blocks, so the translation layer must write elsewhere and remap", "The controller forbids it", "It would exceed the wear limit immediately"], 1, "That single asymmetry produces write amplification, garbage-collection latency spikes and the need for TRIM — all three of the drive's externally visible oddities."],
    ["o12", "What is write amplification?", ["Writing the same data to multiple drives", "Writing 4 KB causing far more than 4 KB of physical writing, because garbage collection must relocate live data", "Amplifying the write signal", "Duplicating writes to a journal"], 1, "Random small writes amplify most and large sequential ones least, which is a real reason log-structured and LSM designs suit flash."],
    ["o12", "An SSD's p99 latency is far above its mean, and worsens as it fills. Why?", ["Thermal throttling", "Background garbage collection competes with foreground I/O, and has less free space to work with", "The interface saturates", "Wear levelling slows reads"], 1, "Over-provisioning gives the collector room, which is why enterprise drives with the same flash sustain far better write performance. The mean says nothing useful about this distribution."],
    ["o12", "A benchmark issues one synchronous read at a time and concludes an NVMe drive is slow. What is wrong?", ["The reads are too small", "It measures latency and only a fraction of throughput — the device is fast only at depth, by Little's Law", "The filesystem adds overhead", "The page cache interferes"], 1, "Concurrency equals throughput times latency, so a queue depth of one caps throughput at 1/latency. Getting depth needs asynchronous I/O or threads."],
    ["o12", "What does increasing queue depth cost?", ["Nothing", "Queueing delay — each request waits behind the others, so throughput-optimised settings make p99 worse", "Memory bandwidth", "Device lifetime"], 1, "Depth converts latency into throughput and adds waiting, so which one you want is an application decision and the default is rarely right for both."],
    // o13
    ["o13", "What did hardware virtualization (VT-x, AMD-V) add?", ["Faster memory access", "A mode beneath ring 0, so a guest kernel runs at its own ring 0 and traps to the hypervisor", "Support for more guests", "Encrypted memory"], 1, "It obsoleted binary translation and paravirtualization, which existed because x86's privileged instructions failed silently in user mode rather than trapping."],
    ["o13", "What is a container, mechanically?", ["A lightweight virtual machine", "A process to which the host kernel gives a restricted view of itself, via namespaces, cgroups, capabilities and seccomp", "A chroot with networking", "A statically linked binary"], 1, "There is no guest kernel and no boot, which is why it starts in milliseconds and its image ships no kernel. \"Lightweight VM\" gets the mechanism exactly backwards."],
    ["o13", "What do namespaces control, and what do cgroups control?", ["Namespaces limit usage, cgroups limit visibility", "Namespaces limit what a process can see; cgroups limit how much it can use", "Both limit visibility", "Both limit usage"], 1, "PID, net, mnt, uts, ipc and user namespaces partition the kernel's views; cpu, memory, io and pids cgroups meter consumption. Capabilities and seccomp then limit what it can do."],
    ["o13", "Why is a container a weaker isolation boundary than a VM?", ["Containers have no memory protection", "A container's boundary is the whole syscall interface, so one kernel privilege escalation escapes every container on the host", "Containers share a filesystem", "Containers cannot use seccomp"], 1, "A hypervisor's interface to a guest is much smaller. That is why untrusted multi-tenant workloads still run in VMs, and why gVisor and Kata Containers exist to narrow the surface."],
    ["o13", "A JVM in a container sizes its heap from the machine's memory and is OOM-killed. What happened?", ["The heap was misconfigured", "Host resource totals leaked into the container, so it read the host's memory rather than its own cgroup limit", "The JVM has a memory leak", "The container had no memory limit"], 1, "This was the long-standing behaviour of /proc/meminfo inside a container. Runtimes are now container-aware and lxcfs papers over it, but any code sizing itself from system totals is worth checking."],
    ["o13", "Why does a container process often ignore SIGTERM?", ["Containers block signals", "PID 1 in a namespace gets no default signal handlers, so a process not written for the role does not terminate", "The orchestrator sends the wrong signal", "The signal goes to the shell instead"], 1, "It is killed after the grace period instead, which is why tini and --init exist. PID 1 also inherits orphans, so one that does not reap accumulates zombies."],
    ["o13", "Why do production systems commonly run containers inside VMs?", ["To improve performance", "Containers give packaging and density; VMs give the tenancy boundary — using both reads where each is strong", "For licensing reasons", "To support multiple kernel versions per container"], 1, "It is a correct reading of the two mechanisms rather than indecision. Containers on one host still cannot run different kernel versions, whether or not they are inside a VM."],
    ["o13", "What did nested paging (EPT/NPT) fix, and what did it cost?", ["It fixed device emulation; it cost bandwidth", "It moved guest-physical to host-physical translation into hardware, at the cost of a longer page walk on a TLB miss", "It fixed CPU scheduling; it cost memory", "It fixed interrupt delivery; it cost latency"], 1, "Shadow page tables in software were the largest cost in early virtualization. Device performance was addressed separately, by virtio and SR-IOV."],
    // o14
    ["o14", "Two threads update adjacent counters and the code gets slower with more threads, with no shared variable. Why?", ["Compiler optimisation", "False sharing — coherence works on 64-byte lines, so the two counters occupy one line and invalidate each other", "Scheduler migration", "Memory bandwidth"], 1, "Padding to a cache-line boundary fixes it. It is the coherence term of the scalability law made concrete, and nothing in the source looks wrong."],
    ["o14", "A large array is initialised in one thread and processed by many on a multi-socket machine. What is the problem?", ["The cache is cold", "Linux allocates on first touch, so the whole array lives on one NUMA node regardless of who processes it", "The array is not aligned", "The threads contend on a lock"], 1, "Remote memory costs roughly twice as much to reach. First-touch-in-parallel, or numactl, puts the pages where the work is."],
    ["o14", "A latency-sensitive service has a bad tail and allocates during each request. What is a likely cause?", ["Garbage collection", "The first touch of newly allocated memory faults, on the critical path", "Lock contention", "Network jitter"], 1, "Pre-faulting, pre-allocation and mlock are standard in low-latency work for exactly this reason. It is the demand-paging mechanism being charged to the request that triggered it."],
    ["o14", "A container at its CPU quota shows periodic latency spikes rather than uniform slowdown. Why?", ["The scheduler is unfair", "A cgroup quota throttles the container for the remainder of each period rather than running it slower", "Other containers are preempting it", "The quota is misconfigured"], 1, "Throttling is a stop, not a slowdown, and it is invisible in average CPU usage — the average looks fine while individual requests hit the throttled window."],
    ["o14", "A benchmark's second run is much faster than its first. What is it now measuring?", ["A warmed-up JIT", "The page cache — it is reading from memory, not from the device", "CPU frequency scaling", "Better branch prediction"], 1, "All four can matter, but for a storage benchmark the page cache dominates by orders of magnitude, and it is why a database maintaining its own cache reaches for O_DIRECT."],
    ["o14", "Throughput against thread count is flat past eight threads. What does that indicate, versus a declining curve?", ["Both indicate memory bandwidth limits", "Flat is Amdahl's serial fraction; declining is coherence cost — and they call for different fixes", "Flat means the benchmark is broken", "Declining means thermal throttling"], 1, "A serial section caps the speedup and holds it there; only coherence traffic makes the curve turn over. Reducing sharing helps the second, and shortening the serial section helps the first."],
    ["o14", "A container is within every cgroup limit it has been given and is still degraded by a co-tenant. How?", ["The limits are not enforced", "cgroups meter CPU and memory well but share last-level cache, memory bandwidth and device queues poorly", "The kernel is oversubscribed", "The container has no CPU pinning"], 1, "That is the noisy neighbour, and it is a genuine gap in what cgroups can partition rather than a misconfiguration."],
    ["o14", "What is the recommended first step when something is slow and the code looks correct?", ["Profile with hardware counters", "Establish whether it is running or waiting — and if waiting, on what", "Check for false sharing", "Increase the thread count"], 1, "Each diagnostic step is cheaper than the next and rules out more, so hardware counters come last. Most investigations that start there are answered by the first question."],
    ["o14", "What is the recurring shape of every leak in this subject?", ["The abstraction is wrong", "The abstraction is not lying, it is charging — and the bill arrives somewhere other than the call site", "Hardware is unpredictable", "Kernels are too complex"], 1, "The address space costs a fault, the file an fsync, the thread a switch and a cache reload, the container a shared kernel. Reading a system means recognising which bill has arrived."]
];
//# sourceMappingURL=questions.js.map