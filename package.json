{
  "name": "kenning",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interactive learning modules — theory, animation, calculator, quiz.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "check": "tsc --noEmit",
    "test": "node tests/smoke.mjs",
    "dev": "python3 -m http.server 8080"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "typescript": "^5.6.0"
  }
}
