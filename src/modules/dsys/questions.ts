import type { Question } from "../../types.js";

/** [moduleId, text, options, correctIndex, explanation] */
export const questions: Question[] = [

// d1
["d1","A node stops replying. What can you conclude from the silence alone?",["It has crashed","It is overloaded","Nothing: a crash, a slow process and a broken link are indistinguishable","The network is partitioned"],2,"Silence is the same evidence in all three cases, which is why a timeout is a decision rather than a measurement. Picking crash, overload or partition all commit to one explanation that the evidence does not support — and an algorithm that acts on the wrong one is unsafe."],
["d1","Which failure model subsumes all the others?",["Crash-stop","Crash-recovery","Omission","Byzantine"],3,"The models nest: crash-stop ⊂ crash-recovery ⊂ omission ⊂ Byzantine. A Byzantine process may do anything, including behaving exactly like a crashed or omitting one, so tolerating it covers the rest. Crash-stop is the narrowest, not the widest; crash-recovery and omission each sit inside Byzantine."],
["d1","Why is crash-recovery harder to reason about than crash-stop?",["Recovery takes longer than a restart","A recovered process may have forgotten what it promised and contradict its own past","Recovered processes run more slowly","It requires more nodes"],1,"A crash-stop process is silent forever, so it can never contradict itself. A recovered one can acknowledge a write, lose it, and come back denying it — which is why these algorithms specify what must reach stable storage before a message is sent. Recovery duration and speed are performance concerns, not correctness ones, and node count is a quorum question, not a failure-model one."],
["d1","What does the partially synchronous model actually assume?",["Message delays are bounded and the bound is known","Bounds exist but are unknown, or hold only after an unknown stabilisation time","Half the messages arrive on time","Clocks are synchronised to within a known skew"],1,"That is the definition: you get eventual good behaviour without ever being told when it started. A known bound is the synchronous model, which no shared network provides. It says nothing about a fraction of messages, and nothing about clocks — clock synchronisation is a separate axis entirely."],
["d1","A client retries a request after a timeout. What has it just guaranteed?",["The operation runs exactly once","The operation may now run twice","The first request was definitely lost","The server will deduplicate it"],1,"The timeout does not prove the first request was lost — it may have been executed with only the reply lost, so the retry can produce a second execution. Exactly-once is not a delivery guarantee but an end-to-end property, and deduplication only happens if someone built it, keyed on a client-generated request id."],
["d1","Which of these is a receive-omission failure's defining hazard?",["The process crashes without warning","The process reports itself healthy while silently missing traffic","The process sends corrupted data","The process runs out of disk"],1,"A receive-omission fault is invisible to the faulty process: logs are clean, metrics are green, and it is missing half the cluster's messages. Self-reported health checks cannot detect it. A crash is a different model, corrupted data is Byzantine behaviour, and a full disk is a local resource failure that the process can see."]
,

// d2
["d2","Two events on different processes have Lamport timestamps L(a) = 7 and L(b) = 9. What follows?",["a happened before b","b happened before a","Nothing about causality — the implication only runs the other way","a and b are concurrent"],2,"a → b implies L(a) < L(b), but not the converse: two concurrent events get whatever values their own histories produced, and one is necessarily smaller. b cannot have happened before a (that would need L(b) < L(a)), but concurrency is not proven either — Lamport values alone cannot distinguish the two cases, which is what vector clocks are for."],
["d2","Which part of the Lamport algorithm is what actually enforces causality across a message?",["Incrementing the counter on every local event","Taking max(L, m.L) + 1 on receive","Attaching the counter to the outgoing message","Breaking ties on process id"],1,"The max drags the receiver's counter past everything the sender knew, so a receive can never be stamped below its send. Local increments only order events within one process; attaching the value is the transport that makes the max possible but does not itself order anything; the tiebreak merely turns the partial order into an arbitrary total one."],
["d2","Two versions of a value carry vector clocks [2,1,0] and [1,2,0]. What is their relationship?",["The first supersedes the second","The second supersedes the first","They are concurrent and the conflict must be resolved","They are identical"],2,"Neither vector is ≤ the other elementwise: 2 > 1 in the first position and 1 < 2 in the second. Supersession would require every entry of one to be ≤ the corresponding entry of the other, and identity would require all entries equal. This is exactly the case a store must not silently collapse."],
["d2","A client reads x = 1 from a node whose clock runs ahead, and because of what it read writes x = 2 on a node whose clock runs behind. What does last-write-wins on wall-clock timestamps do?",["Keeps x = 2, since it was written later","Discards x = 2, although it causally depends on x = 1","Rejects the second write with a conflict error","Keeps both versions as siblings"],1,"The second write carries the smaller timestamp, so the comparison keeps the first and drops the second — an effect ordered before its cause, with success already returned to the client. No error is raised, which is what makes it dangerous; siblings are what a vector-clock store would produce instead, and this one never detects the conflict at all."],
["d2","Which clock should measure how long a local operation took?",["The wall clock, because NTP keeps it accurate","The monotonic clock, which never steps backwards","Either — they differ only in resolution","A Lamport clock"],1,"A wall clock can be stepped backwards by a correction, so subtracting two readings can yield a negative duration. The monotonic clock cannot be compared across machines, but a local duration does not need that. A Lamport clock counts events, not seconds, and cannot express a duration at all."],
["d2","Under happens-before, when are two events concurrent?",["When their timestamps are equal","When neither is reachable from the other by the three rules","Whenever they occur on different processes","When they fall within the clock skew window"],1,"Concurrency is defined as the absence of the relation in both directions — no local ordering, no message path, no chain of the two. Events on different processes are frequently ordered, by messages. And the definition mentions no clock at all, so no skew window enters into it."]
,

// d3
["d3","What does the FLP result actually forbid?",["Any agreement between processes in an asynchronous system","A deterministic algorithm that terminates in every asynchronous execution when one process may crash","Consensus in any system where a process can crash","Consensus without synchronised clocks"],1,"FLP is about guaranteed termination of a deterministic algorithm in every execution, given the possibility of a single crash. Agreement itself remains achievable; consensus is routinely solved in practice and in synchronous or partially synchronous models; and clocks do not enter the statement at all."],
["d3","Which property does a consensus algorithm concede in order to remain correct under asynchrony?",["Agreement","Validity","Termination","Quorum intersection"],2,"Safety — agreement and validity — is held unconditionally, and termination is what is conceded: the algorithm is always safe and eventually live. Conceding agreement would permit split-brain, conceding validity would let the algorithm invent a value, and quorum intersection is an arithmetic fact rather than a property to trade."],
["d3","Why must any two quorums intersect?",["To spread the load evenly across nodes","So that some node present for an earlier decision can report it to a later proposer","To keep the cluster size odd","So that every node sees every message"],1,"The shared member carries knowledge of the earlier decision into the later round, which is what stops two disjoint groups from deciding differently — a split-brain. Intersection has nothing to do with load, and an odd size is merely convenient; no node needs to see every message."],
["d3","A cluster tolerating two Byzantine failures needs how many nodes?",["5","6","7","9"],2,"n = 3f + 1 with f = 2 gives 7, and quorums are 2f + 1 = 5. Five nodes would be n = 2f + 1, which is the crash-fault number; 6 does not satisfy 3f + 1 for f = 2; 9 works but is more than required."],
["d3","In Paxos, what problem does phase 1 solve?",["It commits the proposer's value","It discovers whether a value may already have been chosen, and locks out older ballots","It elects a permanent leader","It replicates the log to followers"],1,"The promise both reports what an acceptor has already accepted and binds it against lower ballots. Committing is phase 2's job; leadership in Multi-Paxos is an optimisation layered on top rather than what phase 1 does; and log replication is Raft's vocabulary, not a Paxos phase."],
["d3","Why does Raft make a voter reject a candidate whose log is behind its own?",["To keep elections fast","Because it guarantees the new leader already holds every committed entry","To balance load across the cluster","Because followers cannot append entries"],1,"The restriction is Raft's substitute for Paxos phase 1: combined with quorum intersection, it makes recovering possibly-chosen values unnecessary, because anyone missing them cannot be elected. It is a safety rule, not a speed or balance one, and followers do append entries — from the leader."],
["d3","What does a leader lease trade away in exchange for a local read?",["Agreement, in favour of availability","A network round trip, in exchange for an assumption about clocks and pauses","Durability of committed entries","The ability to elect a new leader"],1,"The lease replaces the quorum confirmation of leadership with a promise bounded in time, so its correctness now rests on bounded clock error and bounded process pauses — a leader descheduled past its lease can serve a stale read. Agreement and durability are untouched, and a new leader can still be elected once the lease expires."]
,

// d4
["d4","Why does single-leader replication have no write conflicts?",["Followers reject conflicting writes","Exactly one node orders all writes, so there is never a second ordering to reconcile","The leader locks each record before writing","Conflicts are resolved by timestamp"],1,"Conflicts are impossible by construction rather than resolved, which is the main reason single-leader is the right default. Followers do not adjudicate anything, locking is a concurrency-control mechanism inside one node rather than what removes the conflict, and timestamps are what multi-leader systems fall back on precisely because they do have conflicts."],
["d4","A leader acknowledges a write, then crashes before an asynchronous follower receives it. What has happened?",["The write is replayed automatically on failover","An acknowledged write has been lost","The new leader refuses to start","The client receives a delayed error"],1,"Asynchronous replication answers the client before the data has left the leader, so promotion produces a new leader that never saw the write and the client was already told it succeeded. There is nothing to replay it from, the promotion proceeds normally, and no error is ever delivered — which is what makes the loss silent."],
["d4","A user posts a comment, the page reloads, and the comment is missing. Which guarantee was violated?",["Monotonic reads","Read-your-writes","Consistent prefix reads","Linearizability"],1,"They failed to see their own write, because the reload was served by a follower that had not received it. Monotonic reads is about a value appearing and then vanishing across two reads, consistent prefix reads is about causally ordered writes arriving out of order, and linearizability is a far stronger property than this anomaly requires."],
["d4","A user refreshes twice and a comment that was visible disappears. What causes it, and what fixes it?",["Replication lag; wait longer","Two reads landing on followers with different lag; pin each user to one replica","A write conflict; resolve with vector clocks","Clock skew; synchronise with NTP"],1,"Time appears to run backwards because the second read went to a replica further behind than the first, so routing the user consistently — by hashing their id rather than choosing randomly — removes it. Waiting is not a mechanism, no conflicting write occurred, and no clock is consulted in this anomaly."],
["d4","What does multi-leader replication buy, and what does it cost?",["Higher throughput, at the cost of storage","Local write latency and datacentre-level availability, at the cost of write conflicts","Stronger consistency, at the cost of latency","Simpler failover, at the cost of throughput"],1,"That single trade is the whole of multi-leader replication: more than one node ordering writes is exactly what makes conflicting writes possible. It does not strengthen consistency, and failover becomes harder rather than simpler."],
["d4","With N = 5, W = 3, R = 3, what does R + W > N guarantee?",["That reads are linearizable","That every read set contains at least one replica holding the latest write","That no two writes conflict","That the system stays available during a partition"],1,"Intersection is the entire content of the inequality. It is explicitly not linearizability — sloppy quorums, partial writes and replica rebuilds all void the argument — concurrent writes can still conflict, and availability is a separate question."],
["d4","Why is a leaderless configuration with R + W ≤ N particularly hard to catch in testing?",["It fails only under high load","Most reads still return the latest value; only the ones landing on a disjoint set are stale","It fails only during a partition","The staleness is logged but not returned"],1,"A read set that happens to include an up-to-date replica returns the current value, so the configuration looks correct until the read that does not — with nothing different between the round that worked and the one that failed. It is not load- or partition-dependent, and nothing records the staleness."],
["d4","What does a sloppy quorum do to the R + W > N argument?",["Strengthens it, by contacting more nodes","Voids it — W acknowledgements may come from nodes outside the home replica set, so read and write quorums can be disjoint","Nothing; it only affects latency","It applies only to reads"],1,"Accepting writes on any reachable nodes and handing them back later is an availability feature that breaks the overlap guarantee outright. It changes correctness rather than just latency, and hinted handoff affects where writes land, not only reads."],
["d4","Why is read repair alone insufficient to keep replicas converged?",["It is too slow","It only repairs data that someone reads, so rarely-read values drift indefinitely","It cannot detect stale values","It requires a leader"],1,"Cold data is exactly what read repair never reaches, which is why a background anti-entropy process comparing replicas — typically via Merkle trees — is also required. It is essentially free rather than slow, it detects staleness fine, and it needs no leader."],
["d4","What does the CAP theorem actually state?",["A system may pick two of consistency, availability and partition tolerance","During a network partition, a system cannot be both linearizable and available","Consistency and availability are always in tension","Distributed systems cannot be consistent"],1,"Partitions are a property of networks rather than a design choice, so P is not on the menu and the theorem reduces to a choice during a partition. \"Pick two of three\" is the popular misreading, the tension is not constant, and consistency is achievable at a cost."],
["d4","Which word does CAP's C refer to?",["The C of ACID — application invariants","Linearizability","Eventual consistency","Consistent prefix reads"],1,"CAP's C is linearizability, a strong single-object guarantee, and shares nothing but a name with ACID's consistency. Eventual consistency and consistent prefix reads are much weaker properties that CAP does not constrain."],
["d4","What does PACELC add to CAP, and why does it matter more?",["A fourth letter for durability","The else branch — when there is no partition, the choice is latency or consistency","A formal proof CAP lacked","A classification of failure detectors"],1,"The else branch names the trade-off paid on every request for the life of the system, whereas the partition case applies only on the rare days a link fails. It adds no durability dimension, is a reformulation rather than a proof, and says nothing about failure detectors."],
["d4","Spanner provides linearizability across datacentres. What does it pay?",["Nothing — TrueTime removes the trade-off","A commit wait on the clock uncertainty interval, plus the hardware to keep that interval small","Availability during normal operation","Write conflicts that the application must resolve"],1,"It waits out the uncertainty rather than dodging it, which costs milliseconds per transaction and requires atomic clocks and GPS receivers to keep the bound tight — the E branch of PACELC, paid openly. It stays available in normal operation and has no application-visible conflicts."]


];
